# 대사 전문 — 『그리는 자의 바다 / Sea of the Pen』

> **v6** — [STORY.md](STORY.md) v6 구조에 맞춘 전면 개정. 구 기획(v5)의
> 27씬은 전량 폐기했다. 형식만 물려받았다. (제목은 v5 와 같다 — [STORY.md](STORY.md) 머리말 참고)
>
> `[S-##]` 씬 ID 와 뒤의 `` `CODE_CONST` `` 는 **`src/story/script.js` 의 `SCRIPT` 키와 1:1** 이다.
> 한쪽만 고치지 않는다. 비트 순서와 BGM 은 같은 파일의 `INTRO_BEATS` 에 있다.

**표기**
`▶` 연출 지시 · `※` 구현 메모

**말투**
루 = 말이 짧다. 속마음은 괄호.
미오 = 밝게 말한다. 그게 거짓말인 걸 둘 다 안다. **초상화가 없다 — 목소리만.**
모루 = 노인. **겪은 사람이라 단정한다** (~다 · ~지 · ~거라).
포포 = 노인. **전해 들은 사람이라 미룬다** (~구나 · ~다더구나 · ~다만).
세렌 = 젊다. 짧게 툭툭 던진다 (~어 · ~야 · ~지). **바닷새 — 육지 인물은 전부 고양이다.**

※ ★ 모루와 포포는 **둘 다 노인이다.** 겹치지 않는 것은 종결이 아니라 **위치**다 — 포포는
   전설을 자기 말로 보증하지 않고, 모루는 갔다 온 사람이라 보증한다. 고칠 때 이걸 먼저 볼 것.

**제약**
한 줄 = 대화창 한 번. 대화창 폭이 약 33자라 3줄까지 들어간다. **한 줄은 90자를 넘기지 않는다.**
화자를 비우면 내레이션(이름표가 숨는다).

---

## 프롤로그 — 마을과 부두

> 루의 감정선: **막막함 → 결심**

### [S-01] 병상 `SICKROOM`

▶ 배경 `sickroom` (**신규**) · BGM `village`

| 화자 | 대사 |
|---|---|
| — | 사흘째 동생의 열이 내리지 않는다. |
| 미오 | …오빠. |
| 미오 | 오늘은 좀 괜찮아. 진짜야. |
| 루 | 괜찮은 사람은 그 말을 안 해. |

| 화자 | 대사 |
|---|---|
| 미오 | 누워만 있으니까 심심해서. 이거 봐. |
| — | 종이 한 장. 배가 그려져 있다. |
| 미오 | 오빠 배야. |
| 미오 | 바다 건너까지 가보는 게 꿈이었잖아. 이거 타면 어디든 갈 수 있어. |
| 루 | (선이 울퉁불퉁하다. 쥘 힘이 없었던 모양이다.) |

| 화자 | 대사 |
|---|---|
| 미오 | 바람이나 쐬고 와. 나 괜찮아! |
| 루 | (괜찮다는 말은 사흘째고, 목소리는 어제보다 작다.) |

※ ★ **배경을 `village_alive`(야외 마을 전경)에서 신규 `sickroom`(실내)으로 바꿨다.**
   병상 장면이 야외 그림 위에서 돌고 있었다 — 대사와 그림이 서로 딴 데를 보고 있었고,
   이것이 S-01 이 가장 어색했던 이유다. 19종 중 실내는 `workshop` 하나뿐이라 새로 그렸다.
※ 세계를 소개하던 줄("섬 하나뿐인 마을이다…")은 **S-03 으로 내려갔다.** 방 안에서 말하면
   설명이지만 부두에서 말하면 묘사다.

※ 미오는 `sprite` 를 주지 않는다. 얼굴이 없는 것이 연출이다 — 닿을 수 없는 사람이라서 항해가 필요하다.
※ 이 "괜찮아"는 에필로그에서 같은 말로 회수한다 (STORY.md §5).
※ ★ **미오의 종이가 프롤로그의 축이다.** 여기서 받고 S-04 에서 펴고 S-05 에서 그린다.
   설계 화면의 첫 밑그림이 이 종이이므로 튜토리얼이 기능이 아니라 소품에서 나온다.
※ ★ **"선이 떨린다"는 밑그림이 비대칭이라는 사실의 복선이다. 방향은 말하지 않는다** —
   플레이어가 바다에서 배가 한쪽으로 도는 것을 먼저 보고 이 줄을 되짚는 순서여야 한다.
   먼저 알려 주면 놀랄 자리가 없어진다.

---

### [S-02] 불치병과 불가사리 `THE_ONLY_CURE`

▶ 배경 `living_room` (**신규 그림**) 으로 시작 · BGM `tension` 으로 전환
▶ ★ `living_room` 에는 **움직임이 붙어 있다** (`scenefx.js`) — 램프빛의 맥박, 그 빛 속의
  먼지, 그리고 **루의 눈 깜빡임**. 인물이 앉아만 있는 컷이라 가만두면 정지 사진이 된다.
  깜빡임이 제일 크게 먹는다 (분위기가 아니라 살아 있다는 신호라서).
▶ ★ 배경이 아홉 번 바뀐다 — `living_room`(진단) → `bulgasari_deep` → `bulgasari_name`
  → `bulgasari_feed` → `essence` → `living_room`(전설 끝, 다시 지금) → `rock_strait`
  → `night_storm` → `volcano` → `world_end` → `shipyard_grave`(경고)
▶ ★ 미오의 방이 아니라 **거실**이다. 장소 이동이 아니라 배려다 — 나쁜 소식을 미오가 못
  듣는 데서 말하려고 형제를 병실 밖으로 데리고 나온 것이고, 포포의 첫 줄이 "앉아라"인
  것도 거기 소파가 있어서 성립한다.

**진단**

| 화자 | 대사 | 배경 |
|---|---|---|
| 포포 | 루냐. 앉아라. 서서 들을 얘기가 아니야. | `living_room` |
| 포포 | 마을 약재는 다 써 봤는데 드는 게 하나도 없구나.. | |
| 루 | 무슨 병입니까. | |
| 포포 | 나도 모르겠구나. 여태 못 보던 병이야. | |
| 포포 | 고칠 방도가 없구나.. 그런 것을 불치병이라 한단다. | |
| 루 | … | |
| 포포 | …아주 없는 건 아니다만. | |
| 포포 | 믿을지 말지는 네가 정하려무나. | |

**전설**

| 화자 | 대사 | 배경 |
|---|---|---|
| 포포 | 바다 밑에 불가사리라는 게 산다더구나. | `bulgasari_deep` |
| 포포 | 불가살이(不可殺伊). 죽일 수 없는 것이라는 뜻이란다. | `bulgasari_name` |
| 포포 | 가만히 누워서 물이고 배고 고기고 다 빨아들인다지. | `bulgasari_feed` |
| 포포 | 삼킨 것이 몸속에 맺혀 정수가 된다고 하는구나. | `essence` |
| 포포 | 그 정수 한 모금이면 어떤 병도 낫는다고, 오래전부터 이 바다에 전해 오는 이야기란다. | |
| 루 | 그 괴물이 어디 있는지 아시나요. | `living_room` |

**바다 셋**

| 화자 | 대사 | 배경 |
|---|---|---|
| 포포 | 바다를 셋 건너야 하지. | |
| 포포 | 첫째는 바위 협곡. 바람은 등을 밀어주지만, 주변이 온통 바위지. | `rock_strait` |
| 포포 | 둘째는 역풍 협곡. 바람이 정면으로 막아서는데, 해적까지 들끓는단다. | `night_storm` |
| 포포 | 셋째는 불의 바다. 물 대신 용암이 흐르는 곳이지… | `volcano` |
| 포포 | 그 셋을 다 건너면 불가사리의 바다가 나온단다… | `world_end` |
| 포포 | 전설로만 전해지던 그것이 거기 있다더구나. | |

**경고**

| 화자 | 대사 | 배경 |
|---|---|---|
| 루 | 알겠습니다. | |
| 포포 | 알긴 뭘 알아. | |
| 포포 | 가겠다고 나선 사람은 여럿 있었지. | `shipyard_grave` |
| 포포 | 돌아온 사람은 하나뿐이라 하는 말이야. | |

※ ★ **"없다"가 아니라 "하나뿐"이다.** 그 하나가 [S-04] 의 모루다 — 포포는 이름을 대지
   않고, 플레이어가 다음 씬에서 스스로 알아챈다. 여기를 "없다"로 되돌리면 모루가 살아
   돌아온 사실과 부딪혀 **포포가 거짓말한 것이 된다.**
※ 이 줄이 [S-04] 의 "포포한테 들었단다"와 맞물려 포포의 핀잔("알긴 뭘 알아")을 걱정으로
   뒤집는다 — 포포는 루를 말린 것이 아니라 갔다 온 사람한테 미리 보내 놓은 것이다.

※ ★ **이 게임의 전제가 통째로 여기 있다.** 병이 고칠 수 없는 것이 되고, 목적이 생기고
   (불가사리의 정수), 갈 곳 셋이 정해진다. 배경이 아홉 번 바뀌는 이유도 그것이다 —
   말로만 지나가면 지명 나열이지만 그림으로 지나가면 예고편이다. 심사위원이 컷신을
   끝까지 볼지 여기서 갈린다.

※ `bulgasari_name` 은 **그림이 아니라 코드로 찍는다** (`bgscenes.js`). 이름 풀이 한 줄에
   화면을 통째로 내주는 자리라 글자가 주인공이고, 그러면 PNG 로 만들 이유가 없다.
※ `bulgasari_deep` 과 `bulgasari_feed` 는 **같은 그림 한 장**이다. "누워 있다" → "빨아들인다"
   로 넘어가는 두 줄에 그림을 두 장 그리는 대신, 뒤쪽에만 `scenefx` 의 흡입 움직임을 붙였다.
   자세한 것은 `assets/scene/README.md`.

※ ★ **포포의 말투는 전부 60대 어르신이다.** 종결이 `~구나 · ~지 · ~다더구나 · ~다만 · ~야`
   로 끝나고, 단정하지 않고 미룬다. 전설을 자기 말로 보증하지 않는 것이 그 나이의 정직함이다.
   **한 줄이라도 젊은 말투가 섞이면 인물이 흔들린다** — 고칠 때 이 줄을 먼저 볼 것.

※ ★ **병에는 이름을 주지 않는다.** 포포조차 모르는 병이라야 "고칠 방도가 없다"가 무겁고,
   그래야 뒤의 "어떤 병도 낫는다더라"가 **유일한 길**이 된다. 이름을 붙이면 이름 있는 병에
   이름 있는 약이 있을 것 같아져서, 굳이 괴물을 찾아갈 이유가 약해진다.

※ 루의 `…` 는 대사를 안 쓴 것이 아니라 **말을 잃은 것**이다. 물어볼 것이 없어서가 아니라
   물어봐야 소용이 없어서다. 그 침묵이 있어야 포포의 "…아주 없는 건 아니다만"이 산다.

※ ★ **불가사리는 실제 전승이다.** 고려 말 송도에 나타나 쇠붙이를 먹고 먹을수록 커졌다는
   괴물이고, 이름 「불가살이(不可殺伊)」가 곧 *죽일 수 없는 것*이라는 뜻이다. 현대 한국어의
   「불가사리」가 바다 생물 이름이기도 해서, **바다 괴물이면서 죽지 않는 것**이라는 두 뜻이
   이름 하나에 겹친다. 삼키는 방식은 노르드의 하브구파(가만히 누워 배와 고래를 통째로
   삼킨다)에서 빌렸다.

※ ★ **바다 셋의 각 줄은 그 맵에서 플레이어를 죽이는 것을 하나씩 말한다** —
   1장 암초(`physics/obstacle.js`) · 2장 해적 포격(`game/turrets.js`) · 3장 불(`rules/table.json`).
   튜토리얼 텍스트가 아니라 노인의 경고로 말하는 것이 D4 통과 질문("튜토리얼 텍스트 없이")에 맞다.
   ★ **1장 이름이 「바위 협곡」으로 바뀌어도 "바람은 등을 밀어주지만"은 반드시 남는다** —
   설계 문서 §8 의 뼈대가 *1장의 정답(큰 돛)이 2장에서 오답이 되는 반전*이라, 1장에서 순풍이
   빠지면 그 반전이 통째로 사라진다. 이름은 위험(바위)을 말하고, 뒷절이 정답(바람)을 말한다.

※ ⚠ **시한("사흘")은 없앴다.** 루는 기한을 묻지 않고 다 듣고 한 마디만 한다 — 그의 말수에
   맞고, 포포의 핀잔("알긴 뭘 알아")이 곧바로 경고로 이어지는 편이 시한보다 무섭다.
   S-01 의 "사흘째 열이 내리지 않는다"는 *지나간* 사흘이라 그대로 둔다.

※ 마지막 두 줄에서 `shipyard_grave`(가라앉다 만 배들)로 넘어간다. "돌아온 사람이 없다"를
   말과 그림이 동시에 때린다. 이 배경은 S-05 의 `dawn_wreck` 이 조용히 받는다.

---

### [S-03] 배가 없다 `NO_SHIP`

▶ 배경 `harbor` → `harbor_dusk` (**신규**) · BGM `harbor`

| 화자 | 대사 | 배경 |
|---|---|---|
| — | 섬 하나뿐인 마을이다. 배는 대개 남의 것이고, 바다는 늘 남의 일이었다. | `harbor` |
| — | 부두. 배는 많고, 그중 내 것은 없다. | |
| 루 | 한 척만 빌려주세요. 반드시 돌려드리겠습니다. | |
| 뱃사람 | 어디까지 가는데. | |
| 루 | 바다 셋 건너. 불가사리의 바다까지 갑니다. | |

| 화자 | 대사 | 배경 |
|---|---|---|
| — | 밧줄을 감던 손이 멈췄다가, 다시 감기 시작했다. | |
| — | 아무도 대답하지 않았다. | |
| 루 | 어떻게 해야 하지.. | `harbor_dusk` |
| — | 해가 지고 있었다. | |

※ ★ **침묵에 이유를 줬다.** 예전에는 "빌려주세요" 다음이 곧바로 "아무도 대답하지 않았다"라,
   거절이 그냥 인색함으로 읽혔다. 지금은 **목적지를 말하는 순간** 부두가 닫힌다 — 마을은
   불가사리 얘기를 알고 있고, **알기 때문에** 배를 주지 않는다. [S-02] 의 "돌아온 사람은
   하나뿐이라 하는 말이야"를 아무도 다시 말하지 않고 받는 자리다.
※ ★ **뱃사람에게는 `sprite` 를 주지 않는다** (이름표만. 미오와 같은 처리). 새 흉상을 안
   그리려는 절약이 아니라 연출이다 — 한 사람이 거절한 것이 아니라 마을이 거절한 것이라
   얼굴이 붙으면 오히려 좁아진다. 이 인물은 다시 나오지 않는다.
※ ★ **"밧줄을 감던 손이 멈췄다가, 다시 감기 시작했다."가 이 비트의 중심이다.** 거절을
   말로 시키지 않는다 — 멈추는 것이 들었다는 뜻이고, 다시 감는 것이 대답 안 하겠다는 뜻이다.
※ ★ **루는 여기서 결심하지 않는다.** 예전의 "그럼 만든다"는 배 만들 줄도 모르는 사람의
   결심이라 붕 떠 있었다. 막막한 채로 해가 지고, 결심은 [S-04] 에서 모루가 준다.
※ 배경 전환을 마지막 내레이션이 아니라 **루가 막막해하는 줄**에 걸었다. 그림이 먼저 바뀌고
   말이 뒤따라야 감정이 화면으로 번진다.
※ ★ `harbor_dusk` 는 `harbor` 와 **같은 그림이다** (`bgscenes.js`). 수평선 높이·언덕·구름·
   정박선 셋·등대·부두의 좌표와 seed 를 하나도 바꾸지 않았고, 팔레트와 해의 높이만 바꿨다.
   **seed 를 건드리면 언덕선과 구름이 다른 데서 다시 나서 "다른 부두"가 된다.**
   등대에 불이 들어오는 것이 낮과 다른 유일한 사건이고, 그것이 "해가 지고 있었다"를 받는다.

---

### [S-04] 모루의 펜 `THE_SKETCHBOOK`

▶ **밖에서 시작한다** — [S-03] 의 `harbor_dusk` 를 그대로 이어받는다. BGM `village`
▶ "들어와라" 에서 처음으로 실내(`workshop`)가 된다. 그 줄이 곧 문지방이다.
▶ 배경이 다섯 번 바뀐다 — `harbor_dusk` → `workshop` → `world_end`(회상) → `workshop`
  → `the_pen` → `workshop` → `mio_drawing` → `workshop`

| 화자 | 대사 | 배경 |
|---|---|---|
| 모루 | 거기서 뭘 그렇게 오래 서 있는 것이냐. | `harbor_dusk` |
| 루 | (오늘 나한테 말을 건 사람이 처음이다.) | |
| 모루 | 포포한테 들었단다. 동생이 아프다지. | |
| 모루 | 바다 셋 건너에 가고 싶다고. | |
| 루 | …갈 방법이 없습니다. | |
| 모루 | ……불가사리한테 가겠다는 것이냐. | |
| 루 | 네. | |
| 모루 | 들어와라. 문간에서 할 얘기가 아니야. | `workshop` |

**펜**

| 화자 | 대사 | 배경 |
|---|---|---|
| 모루 | 나도 갔었단다. | `world_end` |
| 모루 | 원하는 건 결국 얻지 못했지. 대신 이걸 얻어 왔다. | `workshop` |
| — | 모루가 낡은 펜 한 자루를 꺼낸다. | `the_pen` |
| 모루 | 불가사리의 바다에서 주워 온 펜이다. 그리면 배가 되지. | |
| 모루 | 이게 있었으니 나도 돌아올 수 있었단다. | |
| 모루 | 다시 가기엔 내가 너무 늙었지만 말이야. | `workshop` |

**미오의 종이**

| 화자 | 대사 | 배경 |
|---|---|---|
| — | 모루가 루의 손에 쥔 종이를 본다. | |
| 모루 | 그건 무엇이냐. | |
| — | 루가 종이를 폈다. 배 한 척과, 그 안에 고양이 둘. | `mio_drawing` |
| 모루 | 배를 타고 나가고 싶은 건 진심이냐. | |
| 루 | 네. 동생을 낫게 해서, 같이 이 배를 타고 어디든 가고 싶습니다. | |
| 모루 | 바다는 누가 그렸는지 안 물어. 어떻게 그렸는지만 묻지. | `workshop` |
| 모루 | 진심은 나한테나 통하지, 물에는 안 통한단다. | |

**하나만 지켜라**

| 화자 | 대사 |
|---|---|
| — | 모루가 스케치북을 내민다. |
| 모루 | 그 종이는 옆에 펴 놓고 봐라. 그릴 건 여기다. |
| 모루 | 하나만 지켜라. 네가 설 자리를 먼저 정하고, 그 둘레를 감싸거라. |
| 모루 | 자기 자리를 안 남긴 배는 아무도 못 태운다. |
| 모루 | …돌아오길 바란다. |
| 루 | 감사합니다, 영감님. |

※ ★ **모루는 갔다 온 사람이다.** 펜은 거기서 얻어 온 물건이고, 그래야 제목
   《그리는 자의 바다》가 소품에서 회수된다. 이전까지 "그린 것이 실체가 된다"는 세계의
   물리법칙일 뿐 유래가 없었다.
※ ★ **포포가 미리 알렸다.** 그래서 모루가 먼저 말을 건다 — 마을에서 유일하게 다가온
   사람이 하필 갔다 온 사람이다. ⚠ [S-02] 마지막 줄이 반드시 "하나뿐"이어야 하는 이유다.
※ ★ **루의 속마음 한 줄이 [S-03] 부두의 침묵을 회수한다.** 온 마을이 입을 닫은 날,
   딱 한 사람이 먼저 말을 건다.
※ ★ **첫 여덟 줄은 아직 밖이다.** "거기서 뭘 그렇게 오래 서 있는 것이냐"는 문 밖에 우두커니
   선 사람을 보고 하는 말이라, 배경이 작업장 안이면 대사가 말이 안 된다. 작업장이 부두 옆인
   것은 조선소니까 자연스럽다. **"들어와라" 한 줄이 문지방 역할을 한다.**
※ ★ **회상은 자리를 옮기지 않고 화면으로 넘긴다** (`world_end` 한 줄). [S-02] 에서 포포의
   전설이 그림으로 넘어간 것과 같은 문법이고, 이미 있는 씬이라 새로 그릴 것이 없다.
   ⚠ 모루의 집을 만들려고 `living_room` 을 재사용하면 안 된다 — 그건 **루의 집 거실**이고
   [S-02] 에서 진단을 들은 방이다. 같은 그림이 두 사람의 집이 되면 플레이어가 헷갈린다.
※ ★ **`the_pen` 은 물건 하나가 화면을 통째로 가져가는 컷이다** (`essence` 와 같은 자리).
   그림이 아니라 코드로 찍는다 — 모양은 `bgkit.js` 의 `fountainPen` 하나뿐이고,
   **설계 화면의 커서가 같은 모양을 쓴다** (`draw.css` 의 `--pen-cursor`).
   ⚠ 둘 중 하나만 고치면 "모루가 준 펜으로 그린다"는 연결이 끊긴다. 반드시 같이 본다.
※ ★ **종이를 펼 때 화면도 같이 편다** (`mio_drawing`). [S-01] 에서 본 그 그림이 여기서 눈으로
   회수되고, 다음 줄("진심이냐")을 모루가 **그림을 보면서** 묻게 된다.
※ ★ **"네. 동생을 낫게 해서, 같이 이 배를 타고 어디든 가고 싶습니다."는 [S-01] 의 미오에게
   하는 대답이다** ("이거 타면 어디든 갈 수 있어"). 한 씬 건너뛰고 받으므로 **미오는 이
   대답을 듣지 못한다** — 그래서 돌아가야 할 이유가 된다.
※ ★ **모루는 진심을 묻고, 곧바로 바다는 안 묻는다고 말한다.** 이것이 이 게임의 논지다 —
   시스템은 그림의 진심·솜씨·의도를 하나도 보지 않고 형상만 본다. 구 기획(v5)의
   "진심을 담아 그리면 실체가 된다"를 정면으로 뒤집는 자리라 지우지 말 것.
   ⚠ **"진심을 담아 그려라"류의 줄을 다시 넣지 말 것.** 플레이어가 잘 그려야 하는 줄 알고
   헤매고, "AI 가 그림을 판정하나요"라는 오해를 산다 (게임 안에 AI 는 없다).
※ ★ **미오의 종이는 밑그림이 아니라 옆에 펴 놓고 보는 것이다.** 설계 화면의 템플릿은
   넷(화살촉형·원·캡슐형·하트)이고 그중 미오의 그림은 없다 — 대사를 화면에 맞췄다.
   [S-05] 마지막 줄("종이를 펴고, 펜을 쥐었다")이 그 동작을 그대로 잇는다.
※ ★ **마지막 두 줄이 `canAttachAt` 규칙(주인공을 감싸야 출항)의 전부다.** 튜토리얼 문구가
   아니라 세계의 상식으로 말한다 — D4 통과 질문이 "튜토리얼 텍스트 없이 1장을 클리어하는가"다.
   절단 시 주인공이 자기 조각을 타고 가는 §7.5 동작의 복선이기도 하다.
※ "들어와라. 문간에서 할 얘기가 아니야."는 포포의 첫 줄("앉아라. 서서 들을 얘기가 아니야")과
   **의도된 반복**이다. 이 마을에서 루에게 앉으라고 한 사람이 둘뿐이고, 둘 다 나쁜 소식을
   아는 사람이다.
※ "감사합니다, 영감님."은 프롤로그에서 **루가 처음으로 감정을 밖으로 내는 줄**이다.
※ BGM 은 `village` 로 되돌린다 (재탕이 아니라 회귀다. 출항 직전에 한 번 따뜻한 데로
   돌려놓아야 [S-05] 의 바다가 넓어 보인다).

---

### [S-05] 출항 `DEPARTURE`

▶ 배경 `dawn_wreck` · BGM `sail`

| 화자 | 대사 |
|---|---|
| — | 새벽. 물이 잔잔하다. |
| — | 멀리, 가라앉다 만 배들이 아직 떠 있다. |
| 루 | 불가사리. |
| 루 | 그 정수를 가져온다. |
| — | 미오의 종이를 펴고, 모루의 펜을 쥐었다. |

※ 마지막 줄 뒤에 `draw.html`(설계 화면)로 넘어간다. 컷신은 여기서 끝이다.
※ 시한을 없앤 대신 목적을 두 마디로 못 박는다. 짧은 두 줄이라는 리듬은 그대로다 —
   여기서 처음으로 루가 "간다"가 아니라 **"가져온다"** 고 말한다. 목적어가 생기는 자리다.
※ 배경을 `sea_day`(한낮)에서 `dawn_wreck`(새벽)으로 바꿨다. **첫 줄이 "새벽"인데 한낮
   그림이 떠 있었다.** 이 배경에는 물에 남은 난파선이 있어서, S-02 의 "돌아온 사람이
   없어서 하는 말이야"를 아무도 다시 말하지 않고 받는다.
※ ★ **"연필"이 아니라 "모루의 펜"이다.** 제목이 여기서 한 번 더 울리고, [S-04] 에서 받은
   물건이 곧바로 손에 들린다. 마지막 줄이 "종이를 펴고 / 펜을 쥐었다"인 것은 설계 화면의
   첫 동작(종이를 옆에 펴 놓고 스케치북에 그린다)과 **한 동작으로 이어지기** 위해서다.

---

## 막간 — 바다와 바다 사이

프롤로그가 끝나면 대사는 **인트로처럼 이어 붙지 않고** 바다 하나가 끝날 때마다 하나씩 불려
나온다. 그것을 막간(interlude)이라 부른다.

- **어느 바다 다음인가**는 `src/game/progress.js` 의 `STAGES[].interlude`
- **무슨 대사인가**는 `src/story/script.js` 의 `SCRIPT` (여느 씬과 똑같다)
- **BGM·아이템 지급**은 같은 파일의 `INTERLUDES` 표

재생은 `sail.html` → `index.html?beat=KEY` → `sail.html` 로 돈다. 컷신 재생기를 메뉴 페이지에
둔 이유는 인트로와 같다 — 대사·배경·BGM 스택이 그 문서에만 부팅돼 있다.

---

### [S-06] 시작의 섬 `FIRST_ISLAND`

▶ 배경 `jungle_green` · BGM `village` · **0장(연습 해역)을 클리어하면 재생** → 끝나면 1장
▶ 이 대사 중에 **키(`rudder`)가 배에 달린다** (`INTERLUDES.FIRST_ISLAND.grant`)

| 화자 | 대사 | 배경 |
|---|---|---|
| — | 노를 놓았다. 팔이 후들거린다. | `jungle_green` |
| 세렌 | 어이. 그 배, 노만 달고 왔어? | |
| 루 | …네. | |
| 세렌 | 용감하거나 급했거나 둘 중 하난데. | |
| 루 | 급했습니다. | |
| 세렌 | 그럴 줄 알았어. 나는 세렌. 시작의 섬의 주인이지. | |
| 세렌 | 너는 어디 가는 중이니? | |
| 루 | 바위 협곡으로 가는 중입니다. | |
| 세렌 | 그 노로? 노는 느려. 느린 배는 바위 사이에서 못 피해. | |

**키**

| 화자 | 대사 |
|---|---|
| — | 세렌이 창고에서 무언가를 꺼내 왔다. |
| 세렌 | 키야. 받아 둬. |
| 세렌 | Q 를 누르고 있으면 왼쪽으로, E 를 누르고 있으면 오른쪽으로 꺾일 거야. |
| 세렌 | 노랑 같이 써. 노로 밀고 키로 방향을 잡는 거야. |
| 세렌 | 단, 느릴 땐 아무 일도 안 나. 물이 안 흐르는데 뭘 밀겠어. |
| 세렌 | 빨라지면 그때 듣는다. |
| 루 | 어떻게 빨라집니까. |
| 세렌 | 다음 바다는 바람이 등을 밀어 줘. 받을 게 있으면 받고. |
| 세렌 | 없으면 팔로 가는 거지. |

**해도**

| 화자 | 대사 | 배경 |
|---|---|---|
| — | 세렌이 접힌 종이를 배 위로 던졌다. | |
| 세렌 | 해도야. 여기가 시작의 섬, 다음이 바위 협곡. | **`voyage_map`** |
| 세렌 | Tab 누르면 언제든 볼 수 있어. 실은 것도 거기 다 적혀 있고. | |
| — | 뱃머리를 돌렸다. 바위 협곡 쪽이다. | |

**배를 다시 그린다**

| 화자 | 대사 | 배경 |
|---|---|---|
| 세렌 | 이봐! 그 배로 바로 출발하려고? | `jungle_green` |
| 세렌 | 배를 다시 그려 봐. 이번엔 키도 달아 보고. | |
| 루 | …네. | |
| — | 모루의 펜을 다시 쥐었다. | |

※ ★ **이 씬이 하는 일은 셋이다** — 새 인물 · 키 · 다음 바다 예고. 하나라도 빠지면 앞의
   연습 해역이 그냥 짧은 스테이지가 된다.
※ ★ **세렌은 키를 주기만 한다 — 다는 것은 플레이어다.** 달아 주면 뒤의 "배를 다시 그려 봐"
   와 모순이다. 섬이 **가다가 들르는 곳**이라 그리기 화면이 다시 떠도 "부두로 돌아간" 것이
   아니고, 첫 배가 노만 달고 그린 배라 여기서 한 번 더 그리는 것이 오히려 본론이다.
※ ★ **세렌은 바닷새다.** 육지 인물이 전부 고양이(루·미오·모루)인데 바다 초입에서 처음
   만나는 것이 새라는 사실 자체가 "여기서부터 다른 세계"를 말한다. 흉상은 이미 있다(`seren`).
※ ★ **말투가 포포·모루와 갈린다.** 둘은 노인이라 미루거나 단정하는데, 세렌은 젊고 실무적
   이라 짧게 툭툭 던진다 (`~어 · ~야 · ~지`). 인물 셋이 다 노인 말투면 구분이 안 된다.
※ ★ **키 조작은 실제 동작을 그대로 적는다.** 예전엔 "Q 랑 E, 왼쪽 오른쪽" 이었는데
   무엇을 어떻게 하라는 건지 안 읽혔다. **누르고 있는 동안** 그쪽으로 꺾인다
   (`devices.js` 가 `held.KeyQ`/`held.KeyE` 를 직접 읽는다). 한 번 누르는 게 아니다.
※ ★ **해도(지도)를 여기서 준다.** 항해 화면의 조작 안내판은 연습 해역에서만 뜨므로
   (D4: 1장부터는 화면이 설명하지 않는다), 1장 이후에 `Tab` 을 알려 줄 사람은 세렌뿐이다.
   받기 전에는 `Tab` 이 **아예 안 열린다** (`STAGES[].gear`) — 소품과 기능이 같이 온다.
   ⚠ 지도를 `E` 에 걸면 안 된다 — `E` 는 이미 **키의 우현 조타**라, 꺾을 때마다 창이 뜬다.
※ ★ **화면이 그 해도가 된다** (`voyage_map` 씬 — 그림이 아니라 코드로 찍는다). 여정 전체를
   한 번은 보여 줘야 [S-02] 의 "바다 셋"이 지명 나열에서 지도로 바뀐다. 같은 그림이 항해 중
   `Tab` 장비창에도 뜬다 — 컷과 UI 가 같은 물건이어야 "세렌이 준 그 해도"가 성립한다.
※ ★ **여기서 배를 한 번 더 그린다.** 세렌이 불러 세우는 것이 곧 그리기 화면으로 가는
   이동이다 (`menu/screen.js` 의 `playInterlude` 끝). 첫 배는 **노만** 달고 그렸으므로 —
   연습 해역이 노 젓기를 익히는 바다라 아이템 칸 자체가 없었다 — 방금 열린 키·돛을 직접
   달아 보는 자리가 여기다. "그린 대로 뜨고, 그린 대로 가라앉는다"가 두 번 성립한다.
   ⚠ 이때 진행도를 지우면 안 된다 (`toDraw()` 금지). 설계만 비우고 진행은 그대로 둔다 —
   지우면 방금 올린 것이 되돌아가 연습 해역을 다시 돌게 된다.
※ ★ ⚠ **"느릴 땐 아무 일도 안 나"를 반드시 말해야 한다.** 키의 양력이 `u·|u|` 라 정지·저속
   에서 정확히 0 이다 (D2 실측: 5 m/s 에서 키 53.27° vs 한쪽 노 0.00°). 안 알려주면
   플레이어는 방금 받은 물건을 달고 아무 일도 안 일어나 "고장 났나" 한다.
※ ★ 그래서 **다음 바다가 순풍인 것이 중요하다.** 거기서 빨라져야 키가 그제야 듣는다.
   ⚠ 받을 것(**돛**)의 이름은 대지 않는다 — 설계 문서 §8 은 큰 돛을 플레이어가 스스로
   찾기를 원한다. "받을 게 있으면 받고" 까지만 말한다.
※ 키를 **기본 장치로 되돌리는 것이 아니다.** D2 에서 키를 아이템으로 내린 결정은 그대로다
   (`CLAUDE.md` D2 — 조작을 하나로 합치면 키가 공짜 상위호환이 되어 트레이드오프가 사라진다).

---

### [S-07] 바위섬 `ROCK_ISLE`

▶ 배경 `rock_strait` → `night_storm` → `rock_strait` · BGM `tension`
▶ **1장(바위 협곡)을 클리어하면 재생** → 끝나면 그리기 화면 → 2장(역풍 협곡)

| 화자 | 대사 | 배경 |
|---|---|---|
| — | 바위 사이를 빠져나오자 좁은 섬 하나가 걸려 있었다. | `rock_strait` |
| — | 누가 바위 끝에 앉아 있다. 검은 외투. 왼눈만 금빛으로 타고 있다. | |
| ??? | 거기서 나온 배는 오랜만이군. | |
| 루 | 누구십니까. | |
| 네일 | 네일. 함장이었다. 지금은 여기 앉아 있고. | |
| 네일 | 다음은 역풍 협곡이겠지. 다른 길이 없으니까. | |

**바람**

| 화자 | 대사 | 배경 |
|---|---|---|
| 네일 | 거긴 바람이 등이 아니라 얼굴로 온다. | **`night_storm`** |
| 네일 | 여기까지 돛으로 왔으면, 그 돛이 이제 너를 뒤로 민다. | |
| 루 | 떼라는 말씀입니까. | |
| 네일 | 그건 네가 정해. 나는 바람이 어느 쪽인지만 말해 줄 수 있어. | |
| 네일 | 하나 더. 그 바람은 한자리에 머물지 않아. | |
| 네일 | 다섯을 세면 방향이 돌아. 등이었다가, 옆이었다가, 다시 얼굴이고. | |
| 네일 | 그리고 어둡다. 앞이 안 보이는 게 바위보다 무섭지. | |

**배를 다시 그린다**

| 화자 | 대사 | 배경 |
|---|---|---|
| 네일 | 가기 전에 네 배를 다시 봐. | `rock_strait` |
| 네일 | 여기까지 온 배가 거기서도 맞는 배라는 법은 없다. | |
| 루 | …고맙습니다. | |
| — | 모루의 펜을 다시 쥐었다. | |

※ ★ **이 씬이 하는 일은 하나다 — 2장의 반전을 미리 말해 주는 것.** 설계 문서 §8 의 뼈대가
   "1장의 정답(큰 돛)이 2장에서 오답이 된다"인데, 그 반전을 바다에서 **처음** 겪으면
   배신감이 된다. D4 통과 질문이 정확히 그것을 묻는다 — *"즐거워하는가, 배신감을 느끼는가"*.
   미리 듣고 들어가면 같은 사건이 배신이 아니라 **준비할 기회**가 된다.
※ ★ **네일은 가르치지 않는다.** 세렌은 `Q`/`E` 까지 말해 주지만(첫 바다라 그래야 했다)
   네일은 조건만 말하고 판단을 넘긴다 — *"그건 네가 정해"*. 1장부터 화면이 설명하지 않기로
   한 것(`STAGES[].hints`)의 인물판이고, 그래서 **"돛을 떼라"고 말하지 않는다.**
※ ★ **바람 두 줄은 `STORM_MAP.fields.wind.directionCycle` 과 1:1 이다** (`interval: 5`,
   여덟 방향). 맵 데이터를 고치면 이 대사도 고칠 것 — 안 그러면 인물이 거짓말을 한다.
   "어둡다"도 마찬가지다 (`darkness: 0.58` · `weather.gloom: 0.65`).
※ ★ ⚠ **해적·포격은 말하지 않는다.** 지도의 한 줄(`ROUTE`)에는 남아 있지만 `STORM_MAP` 에는
   아직 포탑이 없다. 없는 것을 인물이 예고하면 그 자리가 빈 것이 곧 버그로 읽힌다.
   포탑이 들어오면 그때 네일에게 두어 줄 준다.
※ ★ **네일은 고양이다** — 루와 같은 골격, 반대의 선택 (`sprites.js` 의 `nail`). 세렌이
   바닷새라 "여기서부터 다른 세계"였다면, 여기서 다시 고양이가 나오는 것은 *"먼저 간 사람"*
   이라는 뜻이다. 흉상은 이미 있다.
※ ★ **첫 두 줄은 이름을 안 준다** (`???`). 이름표가 먼저 뜨면 "검은 외투"와 "금빛 왼눈"이
   설명이 되어 버린다 — 보고 나서 듣는 순서여야 한다.

---

### [S-08] 돌풍섬 `STORM_ISLE`

▶ 배경 `night_storm` → `volcano` → `night_storm` · BGM `storm`
▶ **2장(역풍 협곡)을 클리어하면 재생** → 끝나면 그리기 화면 → 3장(불의 바다)
▶ ★ 이 대사가 **철(`iron`)과 부스터(`booster`)를 연다** (`STAGES[volcano]`)

| 화자 | 대사 | 배경 |
|---|---|---|
| — | 바람이 잦아든 자리에 바위섬 하나. 노를 놓고 배를 댔다. | `night_storm` |
| — | 파란 것이 바위에 붙어 있다. 별 모양이고, 손바닥만 하다. | |
| ??? | …… | |
| 루 | …자는 겁니까. | |
| 블루베리 | 아니. 보고 있었어. 나무배가 오길래. | |
| 블루베리 | 나는 블루베리. 형제가 둘 더 있는데, 걔들은 저 앞에 있어. | |

**철과 부스터**

| 화자 | 대사 | 배경 |
|---|---|---|
| 블루베리 | 다음은 불의 바다야. 물이 아니라 용암이 흐르는 데. | **`volcano`** |
| 블루베리 | 그 배로는 못 가. 나무는 닿기도 전에 타. | |
| 루 | …그럼 어떻게 갑니까. | |
| 블루베리 | 안 녹는 걸로 그려. 철. | |
| 블루베리 | 대신 무거워. 세 배쯤. 배를 처음부터 다시 생각해야 할걸. | |
| 블루베리 | 그리고 하나 더. 노는 나무야. 선체가 버텨도 노는 타서 없어져. | |
| 루 | …그럼 젓지도 못하는데요. | |
| 블루베리 | 그래서 이걸 줄게. 부스터. 쇳덩이라 안 타. | |
| — | 블루베리가 바위 틈에서 쇠뭉치 하나를 밀어냈다. | |
| 블루베리 | 배 안에 붙여. 붙인 쪽으로 밀어 줄 거야. 어디에 붙일지는 네가 정하고. | |

**목적지, 그리고 부탁**

| 화자 | 대사 | 배경 |
|---|---|---|
| 블루베리 | 너는 어디까지 가는데. | `night_storm` |
| 루 | 불가사리의 바다까지 갑니다. | |
| 블루베리 | …… | |
| 블루베리 | 그건 형제들한테 물어봐. 셋이 모여야 하는 얘기야. | |
| 블루베리 | 그래서 말인데, 나 좀 태워 줘. | |
| 루 | …예? | |
| 블루베리 | 나도 화산섬 가야 하거든. 혼자 헤엄치면 한 달인데 너는 반나절이잖아. | |
| 루 | …타십시오. | |
| 블루베리 | 고마워. 안 무거워, 나. | |
| — | 모루의 펜을 다시 쥐었다. | |

※ ★ ⚠ **이 씬은 연출이 아니라 통행증이다.** 불의 바다는 해역 전체가 330° 이고 목재
   발화점이 250° 라, 나무 선체는 **출발선에서 4.2초 만에 무너진다** (벤치 실측). 즉 이
   대사가 여는 두 해금이 없으면 3장은 클리어가 **불가능**하다. 줄이더라도 셋은 남길 것:
   ① 철로 그려라 ② 노는 나무라 탄다 ③ 그래서 부스터.
※ ★ ⚠ **②를 빠뜨리면 함정이 된다.** 기본 노는 `items/defaults.js` 에서 `material: 'wood'`
   로 박혀 있어 **철 선체가 버텨도 노는 탄다.** 남는 장치가 닻뿐이라 잘 그린 철 배가
   출발선에서 표류한다. 벤치가 답까지 적어 두었다 —
   *"철 부스터는 살아남아 노를 잃은 뒤의 추진이 된다"*.
※ ★ **"대신 무거워. 세 배쯤."이 원칙 2 다.** `areaDensity` 가 나무 300 → 철 900 이다.
   이 줄이 빠지면 철이 공짜 상위호환으로 읽히고, `dev_plan` 이 3장을 *"철 재질,
   **무게-흘수 재설계**"* 라고 규정한 것이 통째로 사라진다.
※ ★ **블루베리 혼자다.** 셋을 여기서 다 쓰면 마그마섬에서 다시 만날 때 새로울 것이 없다.
   태워 달라는 부탁이 그 재회를 **플레이어가 만든 일**로 바꾼다 — 데려다준 사람이 루라서
   형제들이 길을 알려 주는 것이 빚 갚기가 된다.
※ ★ ⚠ **불가사리의 바다로 가는 길은 여기서 말하지 않는다.** 블루베리가 형제들에게 미루는
   것이 마그마섬 씬이 존재하는 이유다. 여기서 답하면 그 씬이 사족이 된다.
※ ★ **첫 두 줄은 이름을 안 준다** (`???`) — [S-07] 네일과 같은 처리. 그리고 첫 대사가
   `……` 인 것은 졸린 눈 도트와 1:1 이다 (`sprites.js` 의 `star_blue`).
※ 3인방 이름: **캐럿**(주황) · **애플**(빨강) · **블루베리**(파랑).
   표정이 곧 성격이다 — 캐럿은 말 많고, 애플은 시큰둥하고, 블루베리는 느긋하다.

---

## 예약 — 아직 안 쓴 씬

문자열조차 없다. ID 만 잡아 둔다. 만들 때 이 파일과 `src/story/script.js` 에 **같이** 넣는다.

| ID | `CODE_CONST` | 자리 |
|---|---|---|
| S-09 | `MAGMA_ISLE` | 3장 클리어 — **마그마섬. 3인방이 모인다** (캐럿·애플 + 블루베리). 불가사리의 바다로 가는 길을 알려 준다 — [S-08] 에서 블루베리가 미룬 그 얘기다. 배경 `volcano` |
| S-10 | `ARRIVAL` | **불가사리의 바다 도착**. 배경 `world_end` |
| S-11 | `EPILOGUE` | 귀항. 미오의 얼굴이 처음 나온다 (흉상은 `sprites.js` 의 `mio` 로 준비됨) |

⚠ **S-09 는 지금 넣어도 재생되지 않는다.** 막간은 「다음 스테이지」 버튼에서만 흐르는데
그 버튼이 `hasNextStage()` 로 잠겨 있어서, 불의 바다 뒤에 **스테이지가 하나 더 생기기
전까지** `STAGES[volcano].interlude` 는 죽은 값이다. 불가사리의 바다가 맵이든 컷신이든
`STAGES` 에 들어오는 날 같이 켠다.

---

## 부록: 상황별 짧은 대사 풀

구 기획의 등급별(S/A/B/C/D) 판정 대사 풀은 **폐기했다** — 이 게임에는 그림을 점수 매기는
심사자가 없다. 대신 아래는 전부 **시뮬레이션이 이미 만들어 내는 상태**에 붙는다.
지금 화면에 나오는 문구는 기능적 안내문이므로, 이 풀은 그것을 **대체하는 후보**다 (미구현).

### 설계 화면 — 지금 나오는 문구 (구현됨, `src/draw/screen.js`)

| 상태 | 현재 문구 |
|---|---|
| 초기 · 초기화 | 선체를 그려 주인공을 감싸세요. |
| 선체 확정 | 선체가 확정됐습니다. |
| 너무 작음 | 선체가 너무 작아요. 더 크게 그려 보세요. |
| 너무 큼 | 선체가 너무 커요. 더 작게 그려 보세요. |
| 폐곡선 실패 | 점이 너무 적거나 도형이 무너졌어요. 다시 그려 보세요. |
| 선체 밖 부착 | 선체 안쪽에 붙여야 해요. |
| 완성 | 설계 완성! 항해로 이동합니다… |

### 주인공이 선체 밖일 때 (모루 — 미구현 후보)

- 자기 자리를 안 남겼다. 그 배엔 아무도 못 탄다.
- 선을 다시 봐라. 네가 밖에 서 있다.
- 배부터 그리고 사람을 끼워 넣는 게 아니야. 순서가 반대다.

※ 이 상태는 `canAttachAt` 이 false 인 경우이고, 출항 버튼이 잠긴다. 코퍼스 중 `bowtie`·`crescent`
   가 실제로 여기 걸린다 — **의도된 결과이고 그리기로 고치는 것이 정답이다.**

### 도착 (미구현 후보 — 지금은 `#hud-banner` 의 "도착!" 한 마디)

- 루: 여기까지는 왔다.
- 포포: 돌아온 사람이 없다고 했지. 이제 하나 있다.

### 파손 — 거동으로 먼저 느끼고 문구는 확인용 (미구현 후보)

- 루: (한쪽으로 돈다.)
- 루: (무거워졌다. 물이 들어온다.)
- 모루: 배는 체력이 줄지 않아. 모양이 줄지.

※ **HP 바를 붙이자는 제안이 나오면 이 줄을 근거로 거절할 것** — D3 통과 질문이
   "HP바 없이 거동 변화만으로 피해를 인지하는가"다 (설계 문서 §7.1).

### 절단 = 분리 (미구현 후보)

- 루: 반쪽이 떨어져 나갔다. 아직 내가 선 쪽이 뜬다.
- 루: (떨어진 쪽에 돛이 있었다.)

### 발밑이 잘렸을 때 — 항해 종료 (미구현 후보)

- — 배는 아직 떠 있다. 탈 사람이 없을 뿐이다.

### 재설계 (미구현 후보)

- 모루: 같은 그림으로 두 번 지면 그건 바다 탓이 아니다.
- 모루: 어디가 부러졌는지 봤으면, 거기를 고쳐라.
- 포포: 급하면 크게 그리고 싶어진다. 크면 깊이 잠긴다.

### 2장 — 1장의 정답이 반전될 때 (미구현 후보)

- 포포: 여기선 돛이 짐이야. 바람이 반대로 분다.
- 모루: 어제 통한 걸 오늘도 쓰겠다면, 오늘은 못 간다.

※ 이것이 §8 커리큘럼("직전 챕터의 정답을 다음 챕터의 오답으로 만든다")의 대사판이다.
   D4 통과 질문이 "이 반전을 즐거워하는가, 배신감을 느끼는가"이므로 **미리 예고하는 쪽**으로 썼다.
