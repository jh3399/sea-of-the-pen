# 그리는 자의 바다 (Sea of the Pen)

NAN 2026 — NHN Game × AI 해커톤 **사전 과제** 프로젝트 (3인 팀, 바이브코딩).
그림을 그려 항해하고 싸우는 픽셀아트 바다 모험 게임.

## 마감/제출 (2026-08-10 마감, 이후 수정 불가)

1. GitHub Pages 웹 빌드 + 전체 소스 (커밋 기록 유지 — squash/force push 금지)
2. 30~60초 플레이 영상 (YouTube)
3. 게임 소개 PDF / 4. AI 활용 기술 문서 PDF (도구·프롬프트·에셋 출처 필수) / 5. 팀원 롤 PDF

> **본선은 사전 과제의 연장이 아니다.** 9월 4~6일 판교, **현장에서 주제가 공개되고 48시간** 개발이다.
> 그래서 **사전 과제 빌드는 그 자체로 완결되어야 한다.** 섬을 여럿 벌려놓고 비워두면 미완성작으로 보인다.
> v5에서 섬을 3개로 줄이고 엔딩까지 완주하게 만든 이유가 이것이다.

## 실행

정적 사이트 — 빌드 없음. 로컬 서버로 index.html 서빙:
```bash
npx http-server -p 5210 -c-1
```

## 구조

| 파일 | 역할 |
|---|---|
| `index.html` / `style.css` | 화면(타이틀·컷씬·그리기·항해·섬탐험·전투·엔딩) + HUD·모달·대화 오버레이 |
| `src/main.js` | 흐름 총괄. 순서와 조건만 다룬다 (대사는 script.js) |
| `src/script.js` | **대사 전문 38씬.** 띄어쓰기·줄바꿈을 여기서 관리. 한 줄 90자 이하 |
| `src/world.js` | 세계 데이터: 바다·섬·도안 3장·펜 4종·마을(NPC 키)·수호자 |
| `src/state.js` | 저장 (localStorage `sotp_save_v1`): 조각·펜·마크·소원·클리어·설정 |
| `src/localjudge.js` | **로컬 판정** — 배 품질·도안 정확도·마크. AI 없이 즉시 |
| `src/judge.js` | AI 판정(곁가지): 프록시 → 로컬 개발 키 → mock |
| `src/drawing.js` | 드로잉 캔버스. `setGuide` 밑그림, `toPixelDataUrl(w, ink)` **잉크 색 인자** |
| `src/guides.js` | 따라 그리기 도안 (정규화 폴리라인) |
| `src/sail.js` | 항해 — 그린 배를 직접 조종, 상륙, 나침반. `runSail(opts)` |
| `src/island.js` | 섬 마을 — 옆스크롤 걷기, NPC 대화(Space), 관문. `runIsland(village, ctx)` |
| `src/dialogue.js` | 대화 오버레이 (스타듀식 하단창, 타자기, 초상화, `bg` 필드로 배경 전환) |
| `src/sprites.js` | 24×32 동물 흉상 (문자 그리드). `dev/sprite-sheet.mjs`로 PNG 확인 |
| `src/pixelbg.js` | 배경 엔진 — 씬 크로스페이드. `setScene(key)` |
| `src/bgscenes.js` | 씬 19종의 그림 (`SCENES` 맵) |
| `src/bgkit.js` | 배경 레이어 프리미티브 (하늘·구름·능선·바다·파도·안개·번개·범선…) |
| `src/audio/` | 칩튠 (WebAudio 코드 작곡, 외부 파일 없음): BGM 7곡 + SFX 11종 |
| `dev/bg-gallery.html` | 배경 전체를 한 화면에서 확인 |
| `dev/sprite-sheet.mjs` | 스프라이트를 PNG로 뽑아 확인 (3종 배경 대비 + 행 폭 검사) |
| `docs/STORY.md` | 기획 v5 — 세계관·인물·스테이지·복선 |
| `docs/SCRIPT.md` | 대사 전문 (script.js와 1:1) |

## 스토리 (v5) — 상세는 docs/STORY.md

**400년 전**, 황금 잉크를 지키던 사람이 죽고 두 아들이 남았다.
동생 **모루**는 잉크를 바다에 버리려 했고, 형이 막다가 **동생의 손을 놓쳤다.**
형은 동생의 피가 묻은 손으로 병을 열었고, **피와 황금이 섞이며 잉크가 짙은 자주빛으로 굳었다.**
던진 병이 깨진 자리부터 세상이 굳기 시작했다. 400년째.

이미 바다에 부어진 잉크는 온 바다에 퍼져서, 지금도 **진심으로 그리면 그림이 실체가 된다.**
그중 한 자락이 모루가 조각해둔 **뱃머리**에 닿아 영혼을 붙잡았다 — 그게 **세렌**이다. 기억은 없다.

**현재**, 모루의 후손 **루**의 마을이 굳어간다. 루는 동요가 사실인지 확인하러 유리 숲에 들어가
세렌을 만나고, **바람호 도안 세 장**을 찾아 성수가 있는 황금섬으로 향한다.
형은 **네일**이라는 이름으로 400년째 살아 있다. 목적은 지배가 아니라 **동생을 되살리는 것**이다.

### 핵심 규칙 (어기면 이야기가 무너진다)

- **오염은 검정이 아니라 자주빛 결정화다.** 플레이어가 긋는 선이 검정이라 검정을 악으로 두면 문법이 충돌한다.
  짙은 자주는 멀리서 검게 보여서 사람들이 "검은 함장"이라 부른다 — **색 자체가 피가 섞였다는 증거**다.
  팔레트 `#3a1f3f` → `#5a2f5e` → `#7a4a7c` → 하이라이트 `#c9a0cf`, 금 가는 선은 `#ffd24a`.
- **도안이 3장인 건 실제 선박 도면(선도)이 3면이라서다** — 측면도·반폭도·정면도.
  세 장이 다 모여야 선체가 정의된다. 각 도면의 성질이 그대로 다른 그리기 과제가 된다.
- **펜은 실력이 아니라 재료를 정한다.** `none → wood → iron → gold`.
  나무 펜은 목선, 철 펜은 철갑선. 같은 그림이 펜에 따라 다른 색으로 렌더링된다.
- **네일의 얼굴은 최종전까지 안 나온다.** 프롤로그는 검은 돛의 배(실루엣)만.
- **최종전 규칙이 곧 주제다** — 네일은 베끼기만 하므로 같은 그림을 두 번 쓰면 무효.
- **루만 도안을 모을 수 있다.** 수호자들이 루의 그림에서 모루의 손을 알아보기 때문. 네일은 400년간 못 모았다.
- 보스는 죽이는 게 아니라 **결정을 깨서 색을 되돌린다.**

## 캐스트 (전원 동물 — 사람 없음)

루=크림 태비 고양이 · 모루=늙은 크림 태비(루와 같은 귀·줄무늬) · 네일=검은(실은 자주) 고양이 해적 ·
세렌=별 표식 바닷새(=모루의 영혼) · 촌장/포포=바다코끼리 · 수호자=가르(재규어)·나르(흰 가오리)·툰(흰 새)

## 규칙/컨벤션

- 외부 에셋 추가 시 출처·라이선스 기록 (현재: NeoDunggeunmo 폰트, SIL OFL)
- API 키를 코드/저장소에 절대 넣지 않는다 (프록시 시크릿 또는 localStorage만)
- 커밋은 작은 단위로, 히스토리 유지 (심사 요건). **작업마다 브랜치부터 만든다**
- 대사 수정은 `src/script.js` 한 곳에서. `docs/SCRIPT.md`와 동기화
- 난수는 `bgkit.hash(i, seed)`로 — `Math.random()`은 매 프레임 튄다
- `globalAlpha`를 바꿨으면 1로 되돌린다
- **`[hidden]`은 항상 이긴다** (`style.css` 최상단). `#id`에 display를 주면 hidden이 무시돼
  오버레이가 클릭을 다 먹는 버그가 난다 — 실제로 겪었다

## 진행 상태

- [x] 코어 루프, 저장/이어하기, HUD, 설정·지도·가방
- [x] 배경 19종 + 크로스페이드 · 칩튠 오디오 · 항해 · 섬 탐험 · 도안 밑그림
- [x] 로컬 판정 엔진 (배 품질·도안 정확도·마크)
- [x] 스토리 v5 전면 개정 + 코드 재구성 — **프롤로그부터 엔딩까지 완주 확인**
- [x] 펜 재료 시스템 (`none → wood → iron → gold`)
- [x] 문서 자주빛 결정화 반영
- [ ] **▶ 다음: 배경 3종 자주빛 재작업** (`village_pale` / `crystal_forest` / `fog_pale`)
- [ ] **수호자 3종 도트** — 가르 재작업 + 나르·툰 신규, **팔레트 2벌**(결정 / 색 회복)
- [ ] 흉상 48×64 상향 + 눈 깜빡임
- [ ] 펜 맞대기 연출 (스토리 최대 장치인데 시각화 0)
- [ ] 포격전 미니게임 (현재 컷씬으로 대체)
- [ ] Gemini 판정 튜닝, 프록시 배포, GitHub Pages 배포
- [ ] 제출 문서/영상

## 배경 씬

전환: 대사 줄에 `bg: 'harbor'` 또는 `setScene('harbor')`. 콘솔에서 `__bg('golden_isle')`.
새 배경: `bgscenes.js`에 `draw(ctx, {w,h,sec})` 만들고 `SCENES`에 등록.
레이어 순서 [하늘] → [먼 배경] → [중간] → [바다] → [앞] → [효과]. 확인은 `dev/bg-gallery.html`.

| 키 | 쓰이는 곳 |
|---|---|
| `night_sea` | 타이틀·기본값 |
| `village_pale` / `village_alive` | 나루 마을 (굳은 / 색이 돌아온) |
| `crystal_forest` | 유리 숲 — 오염의 진원지 |
| `fog_pale` | 프롤로그 검은 돛의 배 |
| `sea_day` | 항해·출항 |
| `jungle_green` | 1막 덩굴섬 (가르) |
| `mirror_fog` | 2막 거울섬 (나르) |
| `iceberg` | 3막 얼음섬 (툰) |
| `night_storm` | 돌풍 지대 |
| `golden_isle` | 황금섬 — 최종전·엔딩 |
| `harbor` `dawn_wreck` `fog_black` `jungle_gold` `volcano` `shipyard_grave` `workshop` `world_end` | 예비 |
