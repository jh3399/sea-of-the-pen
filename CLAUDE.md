# 그리는 자의 바다 (Sea of the Pen)

NAN 2026 — NHN Game × AI 해커톤 **사전 과제** 프로젝트 (3인 팀, 바이브코딩).
그림을 그려 항해하고 싸우는 픽셀아트 바다 모험 게임. 그린 그림을 멀티모달 AI가 판정해 게임에 반영된다.

## 마감/제출 (2026-08-10 마감, 이후 수정 불가)

1. GitHub Pages 웹 빌드 + 전체 소스 (커밋 기록 유지 — squash/force push 금지)
2. 30~60초 플레이 영상 (YouTube)
3. 게임 소개 PDF / 4. AI 활용 기술 문서 PDF (도구·프롬프트·에셋 출처 필수) / 5. 팀원 롤 PDF

## 실행

정적 사이트 — 빌드 없음. 로컬 서버로 index.html 서빙:
```bash
npx http-server -p 5210 -c-1
```

## 구조

- `index.html` / `style.css` — 화면(타이틀·컷씬·배그리기·항해·섬탐험·전투·승리) + HUD·모달·대화 오버레이
- `src/main.js` — 게임 플로우 총괄: 타이틀 → 인트로 → [항해 → 섬 마을 → 보스전] 루프, HUD/모달(설정·지도·가방)
- `src/state.js` — 저장/불러오기 (localStorage `sotp_save_v1`): 조각·아이템·클리어·배·바다 위치·설정
- `src/world.js` — 세계 데이터: 바다 크기, 섬 7+최종전 좌표, 도안 조각 7종, 마을(NPC·대사·소품), 보스
- `src/sail.js` — 항해 엔진: 그린 배를 방향키/터치로 조종, 섬 상륙, 나침반. `runSail(opts)`
- `src/island.js` — 섬 탐험(마을) 엔진: 옆스크롤 걷기, NPC 말 걸기(Space), 보스 관문. `runIsland(village, ctx)`
- `src/audio/` — 칩튠 오디오 (WebAudio 코드 작곡, 외부 파일 없음): BGM 7곡 + 효과음. `playBgm/sfx/set*Volume`
- `src/drawing.js` — 드로잉 캔버스 (스트로크, PNG 내보내기, 그림→도트 `toPixelDataUrl`, **도안 밑그림** `setGuide`)
- `src/guides.js` — 따라 그리기 도안 데이터 (정규화 폴리라인)
- `src/judge.js` — AI 판정: 프록시 → 로컬 개발 키(localStorage `DTS_GEMINI_KEY`) → mock 폴백
- `src/config.js` — `JUDGE_ENDPOINT`(프록시 URL), Gemini 모델 폴백 목록
- `src/dialogue.js` — 대화 오버레이 엔진 (스타듀식 하단 대화창, 타자기, 초상화, `bg` 필드로 배경 전환)
- `src/sprites.js` — 코드로 그린 24×32 동물 흉상 (루/모루/네일/세렌/시험관/가르) + `dev/sprite-sheet.mjs`로 PNG 확인
- `src/pixelbg.js` — 배경 엔진: 씬 전환(크로스페이드) + 저해상도 캔버스 관리. `setScene(key)`
- `src/bgscenes.js` — 씬 14종의 그림 (`SCENES` 맵). 키는 아래 배경 표와 1:1
- `src/bgkit.js` — 배경용 픽셀 레이어 프리미티브 (하늘·별·구름·능선·바다·파도·안개·번개·범선…)
- `dev/bg-gallery.html` — 모든 배경을 한 화면에서 애니메이션째 보는 개발용 페이지
- `proxy/` — Cloudflare Worker 판정 프록시 (API 키 은닉용) + 배포 가이드
- `docs/STORY.md` — 스토리라인 전문 (세계관, 캐릭터, 스테이지 설계)

### 캐스트 (전원 동물 — 사람 없음)

루=크림 태비 고양이(주인공), 모루=늙은 크림 태비(루와 같은 귀·줄무늬 — 7섬 가족 그림 복선),
네일=검은 고양이 해적(같은 종족, 반대의 선택), 세렌=별 표식 바닷새, 시험관=바다코끼리,
보스=각 섬의 짐승. NPC 치비도 전부 동물로 그린다.

## 스토리 요약

조선공 모루 영감이 남긴 전설의 배 '바람호'의 도안 7조각. 주인공 루가 배를 그려 출항 →
검은 함장 네일의 습격으로 파손 → 뱃머리 정령 세렌과 함께 배를 다시 그리고 →
섬 7곳의 보스(가르·피라·나르·툰·에코·무나·모루의 그림자)를 무찌르며 도안 7조각을 모아
최종전(검은 손톱호)으로. **전 8스테이지.** 상세는 docs/STORY.md, 대사는 docs/SCRIPT.md.

**핵심 설정**: 모루 영감이 '황금 잉크'를 바다에 부어서, 진심으로 그리면 그림이 실체가 된다.
황금섬이 금빛인 것도 황금이 아니라 그 잉크가 고여 있어서다.
네일의 검은색은 훔쳐서 썩힌 잉크 — 그래서 그는 스스로 못 그리고 남의 그림을 복제만 한다.
엔딩의 보물은 금은보화가 아니라 **바다 그 자체(=잉크)** — 제목이 곧 결말.
주인공 '루'는 '모루'에서 잘려 나온 이름 (7섬에서 밝혀지는 복선).

## 규칙/컨벤션

- 외부 에셋 추가 시 반드시 출처·라이선스 기록 (현재: NeoDunggeunmo 폰트, SIL OFL)
- API 키를 코드/저장소에 절대 넣지 않는다 (프록시 시크릿 또는 localStorage만)
- 커밋은 작은 단위로, 히스토리 유지 (심사 요건)
- 판정 프롬프트 변경 시 `src/judge.js`와 `proxy/worker.js` 양쪽 동기화

## 진행 상태 / 다음 할 일

- [x] 코어 루프 (그리기→판정→데미지), 스토리 프롤로그, 배 HP·반격, 픽셀아트 1차
- [x] 스토리 문서 완성 — STORY.md(8스테이지 설계) + SCRIPT.md(엔딩까지 전체 대사)
- [x] 씬별 배경 시스템 — 배경 14종 + 크로스페이드 전환 (`setScene` / 대사 `bg` 필드)
- [x] 캐릭터 픽셀아트 상향 — 전원 동물 캐스트 24×32 (명암·외곽선)
- [x] 게임 시스템 1차 — 저장/이어하기, HUD(조각 x/7·현재 위치), 설정(음량)·지도·가방 모달,
      칩튠 오디오(BGM 7곡+SFX), 항해(그린 배 직접 조종), 섬 마을 탐험(스타듀식 대화), 도안 밑그림
- [ ] **▶ 다음 작업: 섬 2~7 마을·보스 콘텐츠 채우기 (엔진은 완성 — world.js에 데이터만)**
- [ ] 액션 전투 개편: 무기는 그려서 "장착", 전투는 방향키+공격키 액션으로 (기획 확정됨)
- [ ] 도안 따라 그리기 정확도 판정 (도안 부품 → 배 강화)
- [ ] Gemini 판정 품질 튜닝, 프록시 배포, GitHub Pages 배포
- [ ] 최종전, 제출 문서/영상

### 배경 시스템 (완료 — 사용법)

- 전환: 대사 줄에 `bg: 'harbor'`를 넣거나, 코드에서 `setScene('harbor')` (기본 650ms 크로스페이드).
  `drawShip({ bg })`, 보스 정의의 `bg` 필드도 같은 방식. 브라우저 콘솔에서는 `__bg('volcano')`.
- 새 배경 추가: `src/bgscenes.js`에 `draw(ctx, {w,h,sec})` 함수를 만들고 `SCENES`에 등록.
  레이어 순서는 [하늘] → [먼 배경] → [중간] → [바다] → [앞] → [효과/보정]로 통일한다.
- 확인: `dev/bg-gallery.html` (로컬 서버로 열면 14종이 한 화면에서 동시에 움직인다).
- 주의: 난수는 반드시 `bgkit.hash(i, seed)`로 — `Math.random()`을 쓰면 매 프레임 튄다.
  `globalAlpha`를 바꿨으면 1로 되돌린다. 좌표는 w/h 비율로 (해상도가 화면 비율마다 다르다).

배경 목록 (구현 완료 / 스테이지 2~7은 배경만 있고 게임 로직은 미구현):
| 배경 키 | 쓰이는 곳 | 분위기 |
|---|---|---|
| `night_sea` | 타이틀·기본값 | 밤바다, 달빛 물비늘 |
| `harbor` | S-01 시험 안내 | 낮 부두, 정박한 배들, 등대, 갈매기 |
| `sea_day` | S-02 출항 | 맑은 낮 바다, 순항 |
| `fog_black` | S-02 습격 | 검은 안개, 붉은 포격 |
| `dawn_wreck` | S-03 세렌 만남 | 새벽, 난파 잔해 |
| `jungle_green` | 1섬 시작의 섬 | 초록 정글, 낮 |
| `jungle_gold` | (예비) | 금빛 정글 |
| `volcano` | 2섬 피라 | 붉은 화산, 재 |
| `night_storm` | 3섬 나르 | 캄캄한 밤바다, 번개 |
| `iceberg` | 4섬 툰 | 빙산, 얼어붙은 배들 |
| `mirror_fog` | 5섬 에코 | 짙은 안개, 물 반사 |
| `shipyard_grave` | 6섬 무나 | 쌓인 난파선, 어둠 |
| `workshop` | 7섬 공방 | 낡은 공방, 미완성 배들 |
| `world_end` | 최종전 | 세계의 끝, 검은 손톱호 |
| `golden_isle` | 엔딩 | 금빛 섬 |

### 다음 작업 상세 — 캐릭터 도트 퀄리티 (우선순위 최상)

배경은 레이어 구조로 재작업 완료(하늘 5단 디더 그라데이션, 시차 구름, 능선 실루엣,
원근 바다 밴드, 흩뿌려지는 달빛 물비늘, 3돛대 범선, 앞 레이어 물보라·갈매기).
논리 해상도는 짧은 쪽 200px 기준 (`MIN_SIDE` in pixelbg.js).

남은 것: `src/sprites.js`의 캐릭터.
- 16×16은 너무 작고 단순 → **24×32 등으로 키우고** 명암·외곽선·디테일 추가
- 배경 톤이 씬마다 달라졌으므로, 캐릭터에 어두운 외곽선을 넣어야 어디서든 읽힌다
