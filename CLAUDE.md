# 그리는 자의 바다 (Sea of the Pen)

NAN 2026 — NHN Game × AI 해커톤 **사전 과제** 프로젝트 (3인 팀, 바이브코딩).
2D 탑뷰 물리 퍼즐 항해 게임. 플레이어가 그린 선체 폴리곤이 **그대로 물리량**이 되고,
재질 × 환경 필드의 보편 규칙표에서 해법이 창발한다.

> **이름과 기획은 따로 움직였다.** 기획은 2026-08-05 에 v5(그림을 AI가 판정하는 턴제 전투)
> 에서 v6(물리 퍼즐)로 피벗했지만, **제목은 2026-08-08 에 원래 이름으로 되돌렸다.**
> 레포·Pages 경로가 처음부터 `/sea-of-the-pen/` 였고, "펜으로 그린 것이 실체가 된다"는
> 전제는 v6 에서 오히려 더 문자 그대로 성립하기 때문이다 — 그린 선이 곧 선체 폴리곤이다.
> 중간에 쓰던 가제 《조선소 / Shipwright》는 문서·화면에서 걷어냈다.
> (코드 식별자 `shipwright:handoff` · `window.shipwright` 는 남아 있다. 보이지 않는 이름이라
> 바꿀 이유가 없고, 바꾸면 진행 중인 브랜치와 충돌한다.)
>
> 구 프로토타입은 `archive/` 에 보존(삭제 아님, 히스토리 유지).
> **게임 안에 AI는 없다.** AI는 개발 과정(바이브코딩)에 쓰이며, 제출 요건의
> "AI 활용 기술 문서"는 그 과정(도구·프롬프트·에셋 출처)을 문서화한다.
>
> `develop` 갈래는 피벗을 모른 채 구 기획을 계속 진행했다 (스토리 v5 · 도트 스프라이트 ·
> 씬 배경 · 로컬 판정 엔진). 그 33건을 통째로 `archive/` 로 받아 두었다 — 버리지 않은 이유는
> **에셋이다**: 도트 스프라이트와 씬 배경은 기획과 무관하게 재활용할 수 있다.

## 마감/제출 (2026-08-10 마감, 이후 수정 불가)

1. GitHub Pages 웹 빌드 + 전체 소스 (커밋 기록 유지 — squash/force push 금지)
2. 30~60초 플레이 영상 (YouTube)
3. 게임 소개 PDF / 4. AI 활용 기술 문서 PDF / 5. 팀원 롤 PDF

## 실행

```bash
npm install
npm run dev      # http://localhost:5210/sea-of-the-pen/
npm run bench    # D0~D3 통과 질문 판정 (헤드리스 계측, 138건)
npm run build    # dist/ — Pages 배포 산출물
```

## 설계 원칙 (모든 결정의 상위 규칙)

전문은 `docs/design_doc.md`. 요약하면:

1. **상호작용을 개별 스크립트로 만들지 않는다** — 재질 × 필드의 보편 규칙표만 정의하고
   개별 상호작용은 조합에서 창발시킨다. **맵별 특수 처리 코드는 0줄.**
2. **순수하게 좋기만 한 속성은 없다** — 모든 재질·아이템·형상은 트레이드오프를 가진다.
   밸런싱 중 "항상 정답"이 나오면 즉시 약점을 추가한다.
3. **시스템이 이미 계산한 출력을 버리지 않는다** — 폴리곤·무게중심·충격량을 게임플레이
   입력으로 되먹인다. 파손 지오메트리(§7)가 대표 사례다.

## 구조

```
index.html            ▶ 배포 루트 = 메인 메뉴 + 인트로 컷신 (심사위원이 받는 첫 화면)
draw.html             배 그리기 (스케치북) — 완성하면 sail.html 로
sail.html             항해 (픽셀 그래픽)
harness.html          엔지니어링 하니스 (설계 ↔ 항해 ↔ 세 척 비교 ↔ 파손)
vite.config.js        base: '/sea-of-the-pen/' · 위 넷이 rollup input
src/
  geom/     poly.js(면적·모멘트·주축·핀치 분리) · clip.js(clipper2 어댑터)
  hull/     strokes.js(포인터 캡처) · polygon.js★① · params.js(3대 파라미터)
            decompose.js(볼록 분해) · corpus.js(재현 가능한 테스트 형상)
  items/    defaults.js(기본 장치: 좌현 노·우현 노·닻 ← 부착점이 곧 조향)
            catalog.js(§4.2 아이템 데이터) · attach.js(인스턴스 부착·팔길이)
  field/    field.js(벡터장·스칼라장 샘플러) · forces.js(돛 힘, 순수 함수)
            zones.json(테스트 존 = 필드 정의. 코드 0줄)
  game/     crew.js(주인공 — 카메라 중심이자 도착 판정의 주체) · goal.js(도착 판정)
            turrets.js(포탑 시계·발사 요청) · scoring.js(도착 시간 → 별)
            progress.js(바다의 순서·전체 노선 — 맵을 하나도 모른다) · handoff.js(항해 중 배 고치기)
  rules/    engine.js(§6 논리 엔진 — 규칙을 하나도 모른다) · table.json(규칙 10줄)
  physics/  world.js(planck 어댑터·고정 타임스텝) · body.js(폴리곤→강체) · hydro.js★③
            devices.js(노 스트로크·키·닻·부스터의 힘) · fields.js(필드 적용부)
            predict.js(예측 궤적선)
  damage/   carve.js★②(불리언 차감·절단 판정) · apply.js(충격→재구성)
  render/   view.js(카메라: 추적+줌+fitTo, 회전 금지)
  draw/     screen.js(그리기 화면) · icons.js(픽셀 아이콘·주인공) · templates.js · draw.css
  sail/     screen.js(항해 화면) · render.js(절차적 픽셀 렌더) · sail.css
            map.js(0장 연습 해역 · 1장 바위 협곡 — 순수 데이터. 맵별 코드 0줄)
            voyagemap.js(Tab 지도 — ROUTE 를 읽어 그릴 뿐, 노선을 모른다)
  menu/     screen.js(메뉴·컷신 오케스트레이션 — 대사 0줄) · menu.css
  story/    dialogue.js(대사 오버레이 엔진) · script.js(대사 전문 ← docs/SCRIPT.md 와 1:1)
  scene/    pixelbg.js(씬 크로스페이드) · bgscenes.js(배경 29종) · bgkit.js(레이어 프리미티브)
            sprites.js(24×32 흉상 6종)          ← 구 프로토타입에서 이관한 에셋
  audio/    audio.js(진입점) · tracks.js(BGM 7곡) · sfx.js(11종) · synth.js
                                                  ← 구 프로토타입에서 이관한 에셋
  ui/       metrics.js(계측 HUD, F3) · harness.css
  main.js   하니스 오케스트레이션
scripts/bench.mjs     D0~D3 통과 판정 벤치 (CI 에서도 실행)
archive/              구 Sea of the Pen 프로토타입 (참고용, 빌드 제외)
                      index.html · CLAUDE.md(구 기획 지침) · src/(스토리·항해·섬·
                      로컬 판정·펜 맞대기 컷) · dev/(도트/배경 확인 페이지·PNG 추출)
                      루트 style.css 를 `../style.css` 로 참조한다 — 그래서 그대로 돌아간다
                      ⚠ 도트·배경·오디오는 여기 없다. src/scene · src/audio 로 이관했고
                        아카이브가 거꾸로 `../../src/...` 를 import 한다 (그래서 아직 돈다)
docs/                 design_doc.md(설계) · dev_plan.md(5일 계획표)
                      STORY.md · SCRIPT.md (**v6 — 물리 퍼즐 기획**. 대사는 src/story/script.js 와 1:1)
```

★ = D0 기술 스파이크에서 검증 완료한 3대 리스크 모듈.

## 기술 스택

- **물리**: planck 1.5 (Box2D 2.4 순수 JS 포트). 엔진 의존은 `physics/world.js`·`body.js` 에만.
- **폴리곤 불리언**: clipper2-js. 자기교차 제거(NonZero Union)와 파손 차감(Difference)에 모두 사용.
- **볼록 분해**: poly-decomp(구멍 없을 때) / earcut(구멍 있을 때).
- **빌드**: Vite 8. 정수 좌표 스케일 1 m = 1000 (clipper), 단위계는 m·kg·s, **Y-up**.

## 규칙/컨벤션

- 외부 에셋 추가 시 반드시 출처·라이선스 기록 (현재: NeoDunggeunmo 폰트, SIL OFL)
- 커밋은 작은 단위로, 히스토리 유지 (심사 요건). `git mv` 로 이동, 삭제 금지.
- 캔버스는 Y-down, 물리 세계는 **Y-up** — 변환은 `hull/polygon.js` 한 곳에서만 한다.
- 파손 파라미터 재계산은 **이벤트 시점에만**. 매 프레임 도는 것은 hydro 저항 적분뿐.
- 맵에 예외 코드를 넣고 싶어지면 원칙 1 위반이다. 규칙표를 고쳐라.
- 대사 수정은 `src/story/script.js` 한 곳에서. `docs/SCRIPT.md` 와 **1:1** 로 맞춘다 (씬 ID 포함).
- 화면 간 이동은 **상대경로**(`location.href = 'draw.html'`). `base` 가 `/sea-of-the-pen/` 이라
  절대경로는 로컬에서만 되고 배포에서 404 난다.
- 배경 난수는 `scene/bgkit.js` 의 `hash(i, seed)` 로. `Math.random()` 은 매 프레임 튄다.

## 진행 상태

- [x] **D0 기술 스파이크** — 세 리스크 모듈 + 계측 HUD + 배포 파이프라인.
      실측: 형상 변환 최대 17.5ms(2000점) · 차감 최대 1.3ms · 물리 스텝 0.01ms
- [x] **D1 (8/6): 코어 필 — 그리기와 항해** [가설 A] — `npm run bench` 39/39 통과.
      기본 3종 장치(키·노·닻) / 세 척 동시 주행 / 예측 궤적선 / 흘수 게이지·실시간 오버레이
      ⚠ 통과 질문("조작감 차이를 말로 설명할 수 있는가")은 **사람이 몰아 보고 판정**해야 한다.
      벤치는 그 전제(장치가 설계대로 동작하는가)까지만 보증한다.
- [x] **D2 (8/7): 아이템 창발 조향 + 규칙 엔진** [가설 B·C] — `npm run bench` 71/71 통과.
      좌우 노 스트로크(키 조향 폐기) / §4.1 아이템 통일 모델 / 바람·온도·습기 필드 /
      JSON 규칙표 10줄 + 논리 엔진 / 테스트 존 6종
      ⚠ 남은 사람 판정 둘 — 벤치는 그 전제까지만 보증한다:
      ① **조작감**: `npm run dev` → 코퍼스 불러오기 → 출항 → ↑ 를 꾹 눌러 본다.
         노 튜닝 슬라이더(왼쪽)로 젓는 속도·힘·한계 속도를 돌려 가며 판정한다.
      ② **슬라럼 실통과**: 부착 모드로 좌우 부스터(A·S)를 달고 번갈아 눌러 본다.
         벤치는 지그재그가 성립함까지만(전환 7회, 진폭 4.6°, 누적 1.9°) 보증한다.
- [ ] **▶ D3 (8/8): 파손 지오메트리 + 3맵 + 게임 루프** — `npm run bench` 150/150 통과.
      그리기·항해 화면 분리(`draw.html` → `sail.html`) / 주인공(`game/crew.js`)과 도착 판정 /
      절단 시 소속 조각 판정.
      **계획 밖 추가**: 메인 메뉴 + 인트로 컷신 5비트(`index.html`), 하니스는 `harness.html` 로.
      구 프로토타입의 배경·도트·칩튠·대사 엔진을 `src/scene`·`src/audio` 로 이관해 재사용했다.
      ⚠ 남은 것: 2·3장 맵 · 실패 분석 화면(항적 고스트 + 침몰 사유 배지).
      **0장 「연습 해역」 추가** — 그리기 튜토리얼이 설계 화면만 가르치고 항해 조작은 한
      글자도 안 가르치는데 그 다음이 곧바로 암초 열한 개였다. 골을 뱃머리에서 40° 옆에
      두어 **직진만으로는 못 닿게** 만든 것이 이 맵의 전부다 (조작을 설명하는 대신 하게
      만든다). 클리어하면 [S-06] 시작의 섬에서 세렌이 **키를 달아 준다** —
      설계 화면을 다시 띄우지 않는 이유는 `docs/SCRIPT.md` [S-06] 메모 참조.
      진행은 `game/progress.js` 의 STAGES 배열 하나뿐이고 맵별 코드는 여전히 0줄이다.
- [ ] D4 (8/9, **12:00 콘텐츠 프리즈**): 폴리시 + 배포 + 영상 + PDF 3종 (타이틀은 8/8 에 선반영)
- [ ] D5 (8/10 오전): 예비, 버그 수정만

각 날의 통과 질문과 실패 시 폴백은 `docs/dev_plan.md` 에 있다. 통과 못 하면 다음 날로 못 간다.

## D0 에서 확인된 사실 (다음 단계에서 전제로 쓸 것)

- clipper2 의 `Union(path, NonZero)` 하나로 자기교차가 정리된다. 단 **정점에서 교차**하면
  한 점으로 이어진 핀치 링이 나오므로 `splitAtRepeatedVertices` 로 갈라 큰 로브만 쓴다.
- planck 는 `maxTranslation` 때문에 사실상 120 m/s 속도 상한이 있다. 온전한 선체는 이 덕에
  저절로 안전하지만, **파손으로 생긴 작은 파편은 발산 임계가 79 m/s까지 내려와** 상한 아래다.
  그래서 `hydro.js` 의 감쇠 충격량 클램프가 반드시 필요하다.
- planck 의 fixture 정점 한계는 8이 아니라 **12**(`Settings.maxPolygonVertices`).
- 선미 추력의 모멘트 팔은 선체 길이에 비례하므로, **형상 그대로 두면 길쭉한 배가 더 민첩해진다**
  (§2.1 의도와 반대). `params.js` 의 과장 계수 둘이 이를 뒤집는다 —
  `yawAddedMassGain`(주력, 시작·정지를 모두 둔하게) 과 `angularSlendernessGain`(보조, 최고
  선회율만 낮춤). **불변식: 후자 < 전자.** 넘기면 회전 감속률(dragW/I)이 뒤집혀 길쭉한 배가
  오히려 빨리 멈춘다. `npm run bench` 가 이 불변식과 §2.1 표 전체를 회귀 검사한다.
- 추력·저항은 반드시 **물리 스텝마다**(`onPreStep`) 넣는다. 렌더 프레임마다 넣으면 planck 이
  스텝 후 힘 누산기를 비우는 탓에 조종감이 모니터 주사율에 좌우된다 (240Hz 장비에서 발견).
- `clipper2-js` 는 `@angular/core` 를 peerDependency 로 선언하지만 런타임 import 은 없다.
  `npm audit` 의 high 경고는 이 프로젝트에선 무해 (번들에 Angular 미포함, 확인 완료).

## D1 에서 확인된 사실 (다음 단계에서 전제로 쓸 것)

- **비대칭 창발의 실제 경로는 `items/defaults.js` 의 `sternAnchor` 하나다.** 부착점을 중심선
  (y=0)이 아니라 후미 단면의 y 중앙에서 뽑으면, 비대칭 선체는 노 추력이 팔길이를 얻어
  직진 입력만으로 선회한다 (실측: 20초에 70°, 선회 반경 21 m). 조향 코드는 0줄.
- 그 부착점은 **정점 min/max 로 구하면 안 된다.** RDP 단순화가 만든 미세 비대칭을 그대로
  증폭해, 완전 대칭인 원형 코퍼스가 6.5 cm 의 가짜 오프셋을 얻어 저절로 돌았다. 변을 잘라
  보간하는 `spanAtX` 로 단면을 여러 개 훑어 폭 가중 평균하면 0.6 cm 로 떨어진다.
- 키에 **사향류 항**(ω·x_r 로 인한 받음각 감소)을 넣으면 안 된다. 물리적으로는 더 정확하고
  §2.1 의 "길쭉한 배는 선회 반경이 크다"를 노리게 되지만, 실측하면 (a) 키가 강한 요잉
  감쇠기가 되어 위 비대칭 창발이 70° → 10° 로 눌리고, (b) 선회 반경 순서는 되레 뒤집힌다
  (길쭉한 배가 빨라 u² 보너스를 받는 탓). D1 은 `sin δ` 모델을 쓴다.
  덧붙여 부호 주의: `control.rudder` 는 "좌현 = +" 라 기하각과 방향이 반대다.
- 비대칭 선회는 각가속도가 작아 **몇 초로는 회전 저항과 구분되지 않는다.** 벤치가 20초를
  도는 이유이며, 육안 확인도 짧게 눌러서는 판정할 수 없다.
- §2.1 의 "회전 둔함"은 **선체의 성질**이라 키로 재면 안 된다 (길쭉한 배의 속도 보너스가
  형상 항을 가린다). 벤치는 기준 토크를 직접 넣는 `torqueTest` 로 잰다.
- `predict.js` 는 planck 과 **비트 단위로 일치**한다 (2.5초 오차 0.0000 mm). 같은
  semi-implicit Euler 를 같은 순서로 돌기 때문이고, hydro·devices 의 순수 함수를 공유하는
  한 유지된다. 예측 전용으로 식을 다시 쓰는 순간 깨진다.
- 기본 닻은 static ground ↔ 선체의 `RevoluteJoint` 다. 회전을 막지 않으므로 §4.2 의
  "그 점을 축으로 도는" 거동이 자동으로 따라온다 — 정지 후에도 잔여 회전이 남는 게 정상.
- 자동화 브라우저(확장 프로그램)에서는 탭이 `visibilityState: hidden` 이라 **rAF 가 0 프레임**
  이다. 육안 검증은 사람이 창을 띄워야 하고, 스크립트로 볼 때는 `stepper.advance()` 와
  `render()` 를 손으로 돌려야 한다.

## D2 에서 확인된 사실 (다음 단계에서 전제로 쓸 것)

- **키 조향은 폐기했다.** 키만 전용 양력식(sin δ)과 전용 노브 넷을 갖고 있어 "조향 코드 0줄"의
  유일한 예외였다. 노를 좌우로 나누면 조향이 `τ = −y·F` 하나로 환원된다 — 한쪽만 저으면
  팔길이(노 반폭)를 얻어 돌고, 양쪽을 고르게 저으면 대칭 배는 상쇄돼 직진한다.
  키는 카탈로그의 부착 아이템으로 내렸다. **버리지 않은 이유는 실측된 트레이드오프다**:
  5 m/s 에서 한쪽 노 조향은 `oarMaxSpeed` falloff 때문에 0.00°, 키는 53.27° (원칙 2).
- 노 쌍의 **중심선은 `sternAnchor` 것을 써야 한다.** `sideAnchors` 가 잰 국소 중심을 그대로
  쓰면 단면 하나의 RDP 미세 비대칭이 두 노의 y 합에 그대로 실려, 완전 대칭인 원형 코퍼스가
  1.0 cm 의 가짜 어긋남을 얻어 20초에 10° 돈다 (D1 에서 한 번 잡았던 버그의 재발).
  후미 밴드를 7단면 훑는 `sternAnchor` 를 쓰면 0.4 cm 로 떨어진다.
  ⚠ 여기 적혀 있던 "20초 70.6°, 선회 반경 21 m"는 **D1 시절 값이라 이미 낡았다.** 그 뒤
  `oarStrokePeak` 을 250 → 800 으로 올린 것이 그대로 실린다 (현재 20초 247°, 반경 13 m).
  창발의 성립 여부만 회귀로 고정하고(>30°, R<60 m) 절대값은 튜닝을 따라간다고 볼 것.
- 스트로크는 **임펄스가 아니라 sin² 시간 봉투**여야 한다(0.45s = 27스텝). 그래야 predict.js 가
  같은 순수 함수로 재현해 비트 일치가 유지된다 (봉투 한가운데에서 재도 오차 0.000 µm).
  검증도 활공만 재면 안 된다 — 스트로크 상태 복사가 통째로 빠져도 통과해 버린다.
- 예측선의 입력 가정이 바뀌었다: 스트로크는 **"더 젓지 않으면"**(진행 중 봉투만 재생 후 활공),
  트리거 홀드는 **"계속 누르고 있으면"**. 엣지 입력에 "유지 가정"은 정의되지 않으므로
  케이던스를 지어내면 궤적선이 플레이어의 미래 입력을 상상하는 셈이 된다.
- ★ **스트로크 시계는 배에 하나뿐이다.** 노마다 따로 두면 안 된다. 노잡이의 케이던스는 팔이
  아니라 몸통의 리듬이라 한쪽만 넣든 양쪽을 넣든 사이클 타이밍이 같고, 무엇보다 시계가
  둘이면 **위상이 어긋난 채 영영 고정**된다: ← 로 우현만 젓다가 ↑ 로 바꾸면 쉬던 좌현 노는
  즉시 시작하고 우현 노는 자기 게이트를 기다려, 두 노가 반 사이클 어긋나 번갈아 젓는 꼴이
  됐다 (거동도 틀렸다 — 좌우 토크가 교대로 들어와 직진해야 할 배가 뱀처럼 흔들린다).
  `control.stroke = {t, port, starboard, queued}` 하나로 두면 이 상태 자체가 표현 불가능하다.
  요청도 **사이클 단위**다 — 한 스텝의 요청을 다 모아 한 번에 넘긴다.
  전환 시 진행 중이던 사이클은 한쪽으로 끝나는 게 맞다(젓던 노를 도중에 빼거나 쉬던 노를
  봉투 중간에 넣을 수는 없다). 벤치는 그게 **한 봉투 안에** 끝나는지를 본다 (실측 24/27스텝).
- 이 회귀를 잴 때 요잉률을 긴 구간의 최대−최소로 재면 안 된다. **잔여 회전의 감쇠 폭**이
  잡혀 진동과 구분되지 않는다 (실측: 어긋남이 없는데도 4초 구간에서 0.642°/s 가 나왔다).
  한 사이클 창 안의 진폭으로 재면 0.075°/s 로 떨어지고, 어긋남이 있으면 몇 °/s 가 된다.
- **노 입력은 홀드다.** 눌려 있는 동안 매 물리 스텝 젓기를 요청하고, 회수가 안 끝난 노는
  `startStroke` 가 거절한다 — 홀드가 곧 최대 케이던스가 되어 리듬을 맞추거나 연타할 필요가
  없다. 힘 모델은 그대로라 봉투·활공·타이밍 감쇠는 전부 살아 있다.
  탭도 그대로 되므로(짧게 누르면 게이트가 한 번만 열린다) 홀드 방식은 엣지 방식의 **상위
  집합**이다. 다만 물리 스텝 사이(<16ms)에 눌렀다 뗀 탭은 홀드 Set 만 보면 존재한 적이
  없게 되므로, `tappedStrokes` 래치로 한 스텝은 반드시 반영한다.
  ⚠ 대신 **"언제 젓는가"라는 플레이어 선택은 사라졌다.** 꾹 누르는 것이 항상 정답이다.
  리듬을 되살리고 싶으면 `oarStrokeCooldown` 을 올린다 — 홀드 상태에서 자동으로
  "젓고 활공하기"가 나오되, 그것도 실력이 아니라 연출이 된다.
- 모드를 옮길 때 `heldStrokes` 를 비워야 한다. 안 비우면 새 배가 태어나자마자 저절로 젓는다.
- **연타 실험 (쿨다운 0.3 → 0) 실측** — 지금은 홀드 방식이라 이 문단은 쿨다운 노브를 다시
  돌릴 때 볼 참고 자료다: 게이트 아래에서는 두 모델의 종단 속도가 **완전히
  같다** (1.11 회/s → 둘 다 2.567 m/s, 0.74 → 2.347, 0.50 → 2.013). 쿨다운은 오직 **상한**만
  정한다 — 1.33 → 2.22 회/s, 종단 2.64 → 2.91 m/s (**+10%뿐**). 입력을 67% 더 넣고 얻는
  것이 10% 라서 연타 보상이 사실상 없고, 대신 최대 케이던스에서 듀티비가 1.0 이 되어
  노가 쉬지 않는다 — **최고 속도로 달릴 때 "젓고 활공하기"가 사라진다.**
  체감을 만든 것은 상한이 아니라 **입력 버퍼**(`oarStrokeBuffer` 0.25s)였다. 젓는 중에 누른
  키를 기억했다 게이트가 열릴 때 발사하면 "씹혔다"가 사라진다. 창을 두지 않으면 손을 뗀
  뒤에도 배가 한 번 더 젓는 유령 스트로크가 생긴다.
  되돌리려면 `oarStrokeCooldown` 한 줄이면 되고, 버퍼는 어느 쪽이든 남길 만하다.
- 케이던스 회귀는 **절대 케이던스**(0.5 회/s)로 재야 한다. 게이트 배수로 재면 쿨다운 노브를
  돌릴 때마다 회귀 임계가 같이 흔들려 무엇을 보증하는지 알 수 없게 된다.
- **`oarMaxSpeed`(기본 6.0, 슬라이더 조절)는 배의 최고 속도가 아니라 노깃의 스트로크 속도다.**
  배가 그만큼 빨라지면 노깃이 물을 뒤로 밀지 못해 추력이 0 이 된다 (프로펠러의 전진비 한계와
  같다). 노만으로는 종단이 4.66 m/s 라 이 벽에 점근적으로만 다가가고, 실제로 닿는 것은
  부스터·돛·해류에 실려 갈 때다.
- 감쇠는 `(1 − r²)²` 곡선이다. 선형은 두 군데서 어색했다 — 2 m/s(r=0.56)에 벌써 절반 아래라
  배가 붙기도 전에 노가 먹통이 되고, r=1 에서 기울기 −1 로 내려오다 툭 끊긴다. 곡선은 같은
  자리를 0.49 → 0.56 으로 버티고 상단 기울기가 0 이라 매끄럽게 붙는다.
  ⚠ **r 을 먼저 clamp 한 다음 곡선에 넣어야 한다.** 역젓기는 along < 0 이라 clamp 없이 넣으면
  (1 − r²)² 이 다시 작아져 뒤로 젓는 힘이 고속에서 줄어든다 — 정반대여야 한다.
- **"고속에서 노가 죽는다"는 앞으로 젓기에만 해당한다.** `along = u·dir` 이라 5 m/s 로 달리며
  뒤로 저으면 along = −5 → 감쇠 없음이다. 실측(5 m/s, 2초): 앞으로 젓기는 무입력과 **완전히
  동일**(4.22 m/s, 0.00°)이고, 한쪽 역젓기는 1.84° 선회 + 3.72 m/s 로 제동한다. 노는 빠를 때
  브레이크이자 역방향 조향 수단이 된다 — 조건 분기가 아니라 부호 하나에서 나온다.
  그래도 키가 53.27° 로 압도하므로 키를 아이템으로 남긴 근거는 그대로다.
- **노 튜닝은 하니스 슬라이더로 라이브 조절한다** (젓는 속도 회/s · 젓는 힘 · 한계 속도).
  `DEVICE_TUNING` 을 직접 쓰므로 물리·예측 궤적선·계측이 한꺼번에 따라온다 — 튜닝을 한
  객체에 모아 둔 것의 배당금이다. `estimateOarTerminal()` 이 예상 종단을 띄우는데(실측과
  1% 이내: 4.69 vs 4.66, 7.34 vs 7.35), 60초 몰아보지 않고 노브를 돌리려면 이 값이 필요하다.
- **부착점은 반드시 선체 안이어야 한다** (`canAttachAt`). UI 편의가 아니라 §7.5 가 전제하는
  불변식이다 — `damage/carve.js` 는 아이템을 담은 조각을 `pointInPolygon` 으로 찾고 어느
  조각에도 없으면 탈락시키므로, 밖에 붙은 아이템은 **첫 파손에 무조건 사라진다.** 게다가 그
  전까지는 팔길이만 공짜로 늘어난 치트로 동작한다. 실측: 선체 반대편 끝을 0.5 m 로 스치기만
  해도 밖에 붙은 부스터가 탈락한다. 경계에 **가까운** 것은 막지 않는다 — 현측 끝에 단 부스터가
  잘 떨어지는 것은 §7.5 가 의도한 트레이드오프다.
- 참고: `crescent` 코퍼스는 라벨과 달리 **무게중심이 선체 안**이다 (donut 도 마찬가지 —
  `strokeToHull` 이 구멍을 버리기 때문). "무게중심 선체 밖"을 진짜로 시험하려면 D3 파손이
  만든 조각을 써야 한다.
- ⚠ **노를 세게 하면 §5.2 원칙 1 이 제일 먼저 깨진다.** 한계 6.0 / 힘 800 으로 올리자 기본
  노 종단이 4.66 m/s 가 되어 **8 m/s 순풍의 돛(3.67)보다 빨라졌다** — D3 1장 "천 + 바람 =
  추력, 큰 돛이 정답"이 통째로 뒤집힌다. 벤치가 이 역전을 잡는다(`D3 1장이 성립한다`).
  해결은 **존의 풍속**으로 했다 (8 → 11 m/s, 돛 5.04 > 노 4.66). §5.2 가 "기본 장치는 강풍·
  강해류에서 역부족하도록 산정한다"고 명시하므로 맵 데이터가 원래 그 레버다 — 돛 카탈로그도
  노 튜닝도 건드리지 않는 최소 변경이었다.
- 고속 회귀는 **`oarMaxSpeed` 상대**(×1.4)로 잰다. 5 m/s 로 박아 두면 한계 속도를 올리는
  순간 시험 속도가 벽 아래로 내려가 회귀가 조용히 의미를 잃는다. 케이던스 회귀와 같은 교훈.
- **`oarStrokePeak` 과 `SAIL_TUNING.pressureCoeff` 는 묶여 있다.** 역풍 페널티는 돛 힘과 노 힘의
  **비**로 체감되므로, 감쇠 곡선 때문에 노를 250 → 400 으로 올렸더니 페널티가 38% → 15% 로
  희석됐다. 돛을 1.2 → 3.6 으로 올려 38% 를 복원했다. 한쪽을 건드리면 반드시 다른 쪽 벤치
  케이스를 다시 본다. 종단 속도는 §6 화산대 통과 타이밍(가설 C)의 전제이기도 하다.
- 스칼라장은 **최댓값**으로 합성한다. 합으로 두면 맵 제작자가 소스를 여러 개 겹치는 것만으로
  규칙 임계를 넘길 수 있어 원칙 1이 새는 구멍이 된다. 벡터장(바람)은 합이 맞다.
- 규칙 엔진에서 **같은 틱에 붙었다 꺼진 상태는 이벤트를 내지 않는다.** 젖은 나무배가 화염
  지대에 있으면 매 틱 `wood-ignites` 가 붙이고 `wet-suppresses-fire` 가 끈다. 거동은 옳지만
  그대로 두면 초당 10건씩 쏟아져 D3 실패 분석 화면이 못 쓰게 된다.
- 가설 C 의 판정은 **같은 존·같은 규칙·같은 코드에서 출발점만 다른 두 항해**로 한다.
  물웅덩이(x=15) 경유 배는 45초 생존(x=121), 건너뛴 배는 x=43 에서 연소 파괴. 경유한 배는
  발화 이벤트가 아예 없다. 대가도 있다 — 젓지 않고 표류하면 젖음이 말라 결국 탄다.
- 철의 내화는 **규칙이 없어서** 성립한다. 규칙 부재가 곧 강점이고 areaDensity 900 이 약점이다.
  신규 재질을 넣을 때도 "약점 규칙을 하나 이상 쓴다"가 기준이지 강점 코드를 쓰는 게 아니다.
- 역풍에서 돛은 거리 **38% 손해**다. D3 2장("1장의 정답이 페널티로 반전")이 "돛을 떼라"를
  강제하려면 이 값을 키워야 한다 — `SAIL_TUNING.pressureCoeff` 또는 존의 풍속으로.
- `applyDevices` 의 스텝당 순서는 predict.js 가 그대로 재현한다:
  ① 스트로크 요청 소비 → ② 조타 지연 → ③ 힘 → ④ 적용 → ⑤ 스트로크 시계 전진. 바꾸면 깨진다.

## D3 에서 확인된 사실 (다음 단계에서 전제로 쓸 것)

- **주인공은 아이템이 아니라 `hull.crew` 다** (`game/crew.js`). 형식은 §4.1 아이템과 똑같지만
  아이템은 "잃어도 항해가 계속되는 것"이고 주인공은 정반대다. `hull.items` 에 넣으면 규칙
  엔진이 그를 재질로 보고 태우고(`itemLost`), `nextBind` 가 트리거 키를 배정하고,
  `droppedItemCount()` 가 항해의 끝을 "탈락 아이템 1"로 집계한다.
  대신 §7.5 **소속 폴리곤 판정 하나만** 빌린다 — `carve.js` 가 아이템과 같은
  `smallestContainer` 로 그의 조각을 정하므로 새 판정 코드는 0줄이다.
- **카메라 중심도 도착 판정도 `bodies[0]` 이 아니라 `findCrewBody()` 다.** 절단 뒤 조각 순서는
  clipper 가 정하므로 `bodies[0]` 은 주인공이 없는 잔해일 수 있다. 벤치가 빈 조각을 일부러
  맨 앞에 놓아 이 회귀를 잡는다.
- **도착은 선체가 아니라 주인공이 한다** (`game/goal.js`). 폴리곤 겹침으로 재면 두 가지가
  샌다: ① 뱃머리만 밀어 넣고 클리어 (실측: 반경 5 m 골에 슬루프 정점 6개가 들어간 시점에도
  주인공은 8.00 m 밖) ② 잘려 나간 파편이 표류해 들어가면 클리어 — 주인공은 반대편 조각에서
  가라앉는 중인데도. 둘 다 §7 이 배를 쪼갤 수 있게 만든 순간 실제로 일어난다.
- 설계 화면의 주인공은 **화면 한가운데에 고정**이고 배를 그가 있는 곳에 그린다. 그 자리가
  고정돼 있어야 "어디를 감쌀 것인가"가 형상 설계의 제약이 된다. 판정은 아이템 부착과 **같은
  함수**(`canAttachAt`)를 쓴다 — 선체 밖에 선 주인공은 §7.5 상 첫 파손에 무조건 사라지므로,
  그런 배는 애초에 아무도 태우지 않은 배다.
  실측: 11개 코퍼스 중 `bowtie`·`crescent` 둘이 화면 중앙을 선체 밖에 둔다 (초승달의 노치).
  **의도된 결과다** — 그 둘은 출항이 잠기고, 그리기로 고치는 것이 정답이다.
- ⚠ **주인공 질량은 0 이다.** 게으름이 아니라 원칙 2 의 반대쪽이다 — 모든 배에 똑같이 실리는
  질량은 선택이 아니라서 트레이드오프를 만들지 못하면서 §2.1 의 흘수·종단 속도 기준선만
  통째로 옮긴다. "선원을 더 태우면 노가 늘고 흘수가 깊어진다"를 넣을 때가 이 값을 올릴 자리다.
- 설계 화면의 카메라는 `world = px / PPM` 로 캔버스 픽셀에 못 박혀 있다(`worldToScreen` 에서
  `width` 가 상쇄된다). 그래서 창 크기가 바뀌면 **이미 그린 선체는 같은 픽셀 자리에 남고
  화면 중앙만 옮겨간다** — 주인공이 배 밖으로 나가 출항이 잠길 수 있다. `resize` 핸들러가
  카메라를 다시 잡고 승선 판정을 다시 돌린다. 카메라를 대신 옮기면 안 된다: 그 매핑이 깨지면
  그리는 자리와 커서가 어긋난다.
- 도착 지점(`DEMO_GOAL`)은 **맵 JSON 이 들어오기 전까지의 임시 배치**다 — `startTurretDrill`
  과 같은 처지이고, 맵 포맷이 생기면 `enterSail` 의 `createGoal(...)` 한 줄이 맵 데이터를 읽는다.
- **암초는 화면 한 폭보다 넓게 깔아야 한다.** 항해 화면은 20 px/m 라 한 화면이 대략 70×40 m 다 —
  항로(x=0→150) 주변에만 깔면 y 로 스무 걸음만 벗어나도 빈 바다가 나오고, 암초가 장애물이 아니라
  "옆으로 돌면 그만"인 장식이 된다. `sail/map.js` 는 해역 전체(±75 m)를 61개로 덮는다.
  벤치가 25 m 띠로 썰어 **빈 띠가 없는지**로 잰다 — 개수나 최대 y 로 재면 한쪽에 몰려 있어도 통과한다.
- **해역 경계는 보이지 않는 벽이 아니라 암초 벽이다** (`boundaryWalls`). `createObstacle` 이 이미
  임의의 볼록 폴리곤을 정적 강체로 만들므로 경계 전용 물리·판정 코드는 0줄이고, 재질도 그냥
  `rock` 이라 규칙 엔진·손상 파이프라인이 항로 한복판의 암초와 똑같이 취급한다.
  네 벽은 두께만큼 서로 길게 겹쳐 귀퉁이를 막는다 — 딱 맞게 자르면 모서리에 구멍이 남는다.
  `reef.inward` 는 **렌더 전용 힌트**다: 어느 면이 바다 쪽인지 알아야 그 면만 톱니로 흔들 수 있고,
  ⚠ 톱니는 **바깥으로만** 파고들어야 한다. 안쪽으로 흔들면 배가 지나갈 수 있는 자리에 바위가
  그려져 보이는 가장자리와 멈추는 자리가 어긋난다 (`drawGoal` 이 고리를 판정 반경 그대로
  그리는 것과 같은 이유). 벽은 200 m 를 넘으므로 화면과 겹치는 칸만 훑는다.
- 장애물 컬링은 `sail/render.js#drawObstacle` 안에 둔다 — 호출자는 전부 넘기고 여기서 거른다.
  60개가 넘는 암초 중 화면에 걸치는 것은 늘 몇 개뿐이고, 하나가 18×18 칸이다.
- **"회전이 둔하고 반경이 크다"는 사람 판정에 두 곳을 고쳤다** (조향 코드는 여전히 0줄):
  ① **방향키 자체를 제자리 선회로 바꿨다.** ← = 우현 앞젓기 + 좌현 역젓기. 두 노의 토크가
     **같은 부호로 더해지고**(≈1.45배) 전진 추력은 서로 깎아(0.55배) 반경이 줄되 **속도를
     판다** — 원칙 2 의 대가가 `dir` 부호 하나에서 나온다.
     실측(슬루프 8초): 제자리 R 2.5 m / 1.69 m/s vs 넓은 선회 R 10.5 m / 3.67 m/s.
     ★ 넓은 선회는 **상쇄 규칙**에서 저절로 나온다 — 같은 노에 반대 방향이 겹치면 그 노는
     물에 넣지 않는다(0). ↑ 를 함께 누르면 좌현의 −1 이 +1 과 상쇄돼 우현만 남고, 그것이
     곧 예전의 "한쪽만 젓기"다 (벤치가 비트 일치로 고정). 순항 중엔 이미 ↑ 를 누르고 있으니
     **달리면서 꺾으면 넓게, 서서 꺾으면 제자리**가 규칙 하나에서 나온다 — 외울 조합이 없다.
     ⚠ 처음엔 `precedence`(한쪽 노를 지목한 요청이 이긴다)로 만들었는데, 그러면 병합이
       **키를 누른 순서에 좌우된다** (↓ 먼저면 제자리 선회, ← 먼저면 그냥 후진). 상쇄는
       0 이 흡수원소라 교환법칙이 성립해 그 상태가 **표현 불가능**하다. 규칙으로 막는 것보다
       낫다. 벤치가 순서를 뒤집어 비트 일치를 확인한다.
     ⚠ 벤치 케이스는 반드시 `STROKE_KEYMAP` 을 거쳐 만들 것. 손으로 쓴 요청 배열을 넣으면
       키맵도 상쇄 규칙도 통째로 빠져도 통과한다.
     ⚠ ← + → 를 동시에 누르면 양쪽이 상쇄돼 **아무 노도 물에 들어가지 않는다** (무입력과
       비트 일치). 의도된 결과이고 벤치가 고정한다.
  ② `yawAddedMassGain` 0.45 → 0.30. **응답성의 주력 노브는 여기다** (키가 아니다).
     같은 기준 토크 3초에 슬루프가 51.2° → 63.1°, 한쪽 젓기 반경 13.3 → 10.5 m.
     §2.1 의 순서(길쭉할수록 둔함)는 그대로고 폭만 좁아진다 — 둥근 배는 165° 로 불변.
     불변식(`angularSlendernessGain` 0.2 보다 클 것) 여유가 0.25 → 0.10 으로 줄었으니
     더 내리려면 0.2 를 같이 내려야 한다.
- **키를 기본 장치로 되돌리는 것은 이 문제의 답이 아니다.** 양력이 `u·|u|` 라 정지·저속에서
  정확히 0 이고 `rudderRate` 2.0 rad/s 의 조타 지연까지 있어, "출발이 둔하다"는 체감은 오히려
  나빠진다. 키가 고치는 것은 **고속 선회 반경뿐**이고 그건 이미 §5.2 원칙 2 의 트레이드오프
  대상이다. 조작을 하나로 합치면 키가 공짜 상위호환이 되어 그 트레이드오프가 사라진다.
