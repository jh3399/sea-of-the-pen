# 컷신 배경 그림 — 출처와 재생성 방법

제출 요건의 "AI 활용 기술 문서"가 **에셋 출처**를 요구하므로 여기 적어 둔다.
타이틀 배경은 `assets/menu/README.md` 에 따로 있다.

## 출처

| 파일 | 무엇 | 출처 |
|---|---|---|
| `source/sickroom-1672.png` | 원본 1672×941 · 30751색 · 1290KB | **ChatGPT 생성**, 2026-08-09 |
| `sickroom.png` | 배포용 1672×941 · 256색 · 440KB | 원본을 `scripts/bg-snap.mjs` 로 가공 |
| `source/mio-drawing-1448.png` | 원본 1448×1086 (4:3) · 1967KB | **ChatGPT 생성**, 2026-08-09 |
| `mio-drawing.png` | 배포용 1930×1085 · 256색 · 860KB | 위를 16:9 로 덧대고 가공 |
| `source/living-room-1672.png` | 원본 1672×941 · 1774KB | **ChatGPT 생성**, 2026-08-09 |
| `living-room.png` | 배포용 1672×941 · 256색 · 469KB | |
| `source/bulgasari-deep-1672.png` | 원본 1672×941 · 2273KB | **ChatGPT 생성**, 2026-08-09 |
| `bulgasari-deep.png` | 배포용 1672×941 · **128색** · 828KB | 아래 참고 |
| `source/essence-1672.png` | 원본 1672×941 · 1199KB | **ChatGPT 생성**, 2026-08-09 |
| `essence.png` | 배포용 1672×941 · 128색 · **224KB** | 거의 검정 + 청록이라 색이 잘 줄어든다 |
| `source/star-isle-1672.png` | 원본 1672×941 · 2038KB | **ChatGPT 생성**, 2026-08-09 |
| `star-isle.png` | 배포용 1672×941 · 256색 · 521KB | [S-09] 별의섬 |
| `source/bulgasari-asleep-1672.png` | 원본 1672×941 · 2443KB | **ChatGPT 생성**, 2026-08-09 |
| `bulgasari-asleep.png` | 배포용 1672×941 · 256색 · 806KB | [S-10] 잠든 불가사리 |
| `source/bulgasari-gullet-1672.png` | 원본 1672×941 · 2557KB | **ChatGPT 생성**, 2026-08-09 |
| `bulgasari-gullet.png` | 배포용 1672×941 · 256색 · 742KB | [S-11] 목구멍 속 보물 |
| `source/essence-handoff-1672.png` | 원본 1672×941 · 1371KB | **ChatGPT 생성**, 2026-08-09 |
| `essence-handoff.png` | 배포용 1672×941 · 256색 · 413KB | [S-11] 블루베리가 정수를 건넨다 |

> `bulgasari-asleep` 은 **눈을 감고 있는 것**이 그림의 전부다. [S-02] 가 이름 풀이
> (불가살이 = 죽일 수 없는 것)에 화면을 통째로 내줬으므로, 엔딩에서 죽이지 않고 **재웠다**는
> 사실을 대사가 아니라 이 한 장이 말한다. 눈을 뜬 버전으로 바꾸면 이야기가 어긋난다.

> `star-isle` 은 **별이 하늘이 아니라 바위에 붙어 있는 것**이 그림의 전부다. 그 별들이 곧
> 캐럿·애플·블루베리가 나온 곳이라 색도 3인방과 같은 셋(주황·빨강·파랑)이다.
> 256색을 유지한 근거는 수평선의 붉은 잔광이다 — 불의 바다에서 넘어온 낙차를 그 한 겹이
> 말하므로, 줄여서 띠가 생기면 "건너왔다"가 사라진다.
> ⚠ 처음에 `star-isle-1672.png.png` 로 확장자가 두 번 붙어 있었다. 저장할 때 볼 것.

> `bulgasari-deep` 만 128색이다. 거의 단색 청색 화면이라 색을 줄이기 쉬울 것 같지만
> 반대다 — **빛줄기의 완만한 그라데이션이 화면의 주인공**이라 64색에서는 눈에 띄는 띠가
> 생겼다. 256색(1131KB)과 128색(828KB)은 구분되지 않아 128 로 정했다.
> **색 수는 색의 가짓수가 아니라 그라데이션의 완만함으로 정한다.**

> `bulgasari-deep.png` 는 **씬 키 둘이 나눠 쓴다** — `bulgasari_deep`(가만히)과
> `bulgasari_feed`(빨아들임). 대사가 "누워 있다" → "빨아들인다" 로 넘어가는데, 그림을
> 두 장 그리는 대신 `scenefx` 의 흡입 움직임만 뒤쪽에 붙였다.

> ⚠ 생성형 AI 산출물이다. 게임 **안**에는 AI 가 없고(설계 원칙) AI 는 개발 과정에만
> 쓴다는 것과 별개로, 이 그림은 AI 가 만들었으므로 기술 문서에 그렇게 적어야 한다.

## 재생성

```bash
node scripts/bg-snap.mjs assets/scene/source/sickroom-1672.png --grid 1672 --colors 256 --out assets/scene/sickroom.png
```

```bash
node scripts/bg-snap.mjs assets/scene/source/mio-drawing-1448.png --aspect 16:9 --grid 1930 --colors 256 --out assets/scene/mio-drawing.png
```

### ⚠ 비율이 다르면 --aspect 로 덧댄다 (자르지 말고)

`mio-drawing` 원본은 4:3 이라 16:9 화면에서 `object-fit: cover` 가 위아래를 잘라
**종이 윗변 9px 이 날아갔다**. `--aspect 16:9` 는 가장자리 픽셀을 늘려 비율을 맞춘다.

이 그림들의 배경이 **가로 판자벽**이라 좌우로 늘리는 것은 이음매가 없다 — 각 행의 끝 색을
옆으로 늘리면 판자선이 그대로 이어진다. 반대로 위아래로 덧대야 할 때는 판자선이 번지므로
결과를 반드시 눈으로 볼 것.

### ⚠ 축소하지 않는다 — 한 번 틀렸던 자리다

처음에 타이틀 배경과 같은 `--grid 418` 로 줄였다가 **그림이 통째로 깨졌다.**
벽 결이 줄무늬 노이즈가 되고 고양이의 눈·코가 뭉갰다.

원인은 `--probe` 를 잘못 믿은 것이다. `--probe` 는 **1672 를 나눠떨어지게 하는 값**을
찾을 뿐 원본이 실제로 몇 도트로 그려졌는지는 모른다. 1672÷418 = 4.00 이 깔끔해 보였지만,
이 그림은 **1px 격자로 그려진 도트**였다 (런 길이 최빈값 2~3px, 8배 확대하면 눈·수건·
외곽선이 전부 1px 이다). 4px 로 줄이는 것은 디테일을 4분의 1로 버리는 짓이었다.

**원본의 진짜 격자를 재는 법**: 가로로 훑어 색이 바뀌는 간격의 최빈값을 본다.
`--probe` 의 정수 후보는 그 다음에 쓴다. 최빈값이 1~3px 이면 축소하지 말 것.

### --colors 는 축소와 다른 문제다

AI 가 뱉는 "픽셀아트"는 모양은 1px 로 그려 놓고 색은 3만 개인 경우가 많다.
그 색의 대부분은 면 안의 미세 노이즈이고, **그 노이즈가 깨져 보이는 것의 절반**이다.
축소로 지우려 들면 모양까지 같이 버리므로, 해상도는 그대로 두고 팔레트만 줄인다.

실측: 30751색 1290KB → **256색 440KB**, 눈으로 구분되지 않는다.

⚠ 더 줄이면 **면적은 작지만 결정적인 색**이 큰 면에 흡수된다. 64색에서 고양이의
초록 눈이 회색이 됐다. 128 이하로 내릴 때는 반드시 눈으로 확인할 것.

## 화면에서

`src/scene/bgphotos.js` 가 씬 키 → 그림을 잇고, `src/scene/pixelbg.js` 의 `setScene()` 이
`#bg-photo` 레이어의 src 와 페이드를 관리한다.

**절차적 배경(`bgscenes.js` 의 같은 이름 씬)을 지우지 않는다.** 두 가지 이유다:

1. 그림이 못 뜨면(파일 누락·네트워크 실패) 아래 캔버스가 그대로 배경 노릇을 한다.
2. 씬 전환 크로스페이드는 캔버스 쪽이 계속 담당하므로, 그림 씬 ↔ 절차 씬 사이가 끊기지 않는다.

⚠ `#bg-photo` 의 CSS 페이드 시간은 `pixelbg.js` 의 `FADE_MS` 와 **같아야 한다.**
다르면 전환 중간에 두 배경이 겹쳐 보인다.

⚠ 경로를 문자열로 적지 말고 `import` 로 가져올 것. Vite 가 해시를 붙여 번들에 넣어 준다 —
문자열은 dev 에서만 되고 배포에서 404 난다 (`base` 가 `/sea-of-the-pen/`).
